# CLAUDE.md

Živý dokument. Průběžně aktualizuj, udržuj krátký a přehledný.

## Projekt
- Název: **DroidShop.cz**
- Firma: Miroslav Opletal — multi-tenant SaaS e-shopová platforma (typ Shoptet / Eshop-rychle)
- Klienti: **nájemci** (provozovatelé e-shopů) + jejich **koncoví zákazníci** (s námi nemají smluvní vztah)
- Popis: Registrovaný uživatel si za měsíční poplatek pronajme e-shop, naplní produkty a provozuje pod vlastní značkou. Platforma dodává software a infrastrukturu; za obsah, ceny, VOP vůči zákazníkům a daně odpovídá nájemce.
- Cíl MVP: do **10 minut** od registrace funkční e-shop na subdoméně, produkty, první objednávka.
- Brand tón: moderní, technický, spolehlivý (doplnit vizuál později)
- Jazyky UI: Čeština (primární); vícejazyčnost storefrontu = post-MVP
- Zdroj pravdy (produkt): [`docs/specs/2026-07-17-eshop-platforma-specifikace.md`](docs/specs/2026-07-17-eshop-platforma-specifikace.md)
- Repozitář: https://github.com/mopix/droidshop.cz
- Šablona AI workflow: https://github.com/mopix/claude-laravel-vue (+ vzor struktury z WooShop)

## Uživatel
- Skill level: pokročilý — používej technický žargon, navrhuj alternativy

## Stack
- Framework: Laravel 13 + Vue 3 + Inertia.js (admin) + TypeScript (postupně)
- Storefront (cíl): Blade SSR + Alpine/Vue ostrůvky (SEO) — viz specifikace §4.1
- UI: Tailwind CSS; shadcn/ui prvky dle potřeby
- Databáze: MySQL 8 / MariaDB (sdílená DB + `tenant_id`) + Redis (cache, fronty, session)
- Multi-tenancy: `stancl/tenancy` nebo `spatie/laravel-multitenancy` (rozhodnutí při implementaci jádra)
- Moduly: `nwidart/laravel-modules` + vlastní vrstva (manifest, per-tenant aktivace)
- Auth: Laravel Breeze (aktuálně v skeletonu); Fortify možná později
- Platby nájemců (platforma): karta / opakované předplatné (Stripe nebo ekvivalent — rozhodnout)
- Platby na e-shopech tenantů (MVP): dobírka, převod (+ QR), 1 brána (Comgate nebo GoPay)
- Storage: **lokální disk pro MVP** (rozhodnutí 2026-07-19, viz sekce Rozhodnutí); `FileStorage` služba drží abstrakci, přechod na S3 = změna configu
- Monitoring (cíl): Sentry + stavová stránka; Telescope jen dev
- Hosting: vlastní VPS

**Profil stacku (stručně):** [`docs/PROJECT-PROFILE.md`](docs/PROJECT-PROFILE.md)

## Role
| Role | Popis |
|------|--------|
| `SUPERADMIN` | Správa platformy, tenantů, tarifů, modulů, fakturace nájemcům |
| `TENANT_ADMIN` | Provozovatel e-shopu — produkty, objednávky, nastavení |
| `TENANT_STAFF` | Personál (post-MVP; datový model s rolemi od začátku) |
| `CUSTOMER` | Koncový zákazník tenanta (guest nákup + volitelný účet) |

**Při implementaci nové funkce se VŽDY zeptej:** která role ji smí vidět/upravovat, nebo zda je veřejná (storefront).

## Pravidla

### Prostředí
- NIKDY needituj `.env` — používej pouze `.env.local` (nebo doplň `.env.example`)
- Komunikace v chatu: česky
- Kód (proměnné, komentáře, commity): anglicky
- Dev server: `php artisan serve` + `npm run dev` (nebo `composer run dev`)
- Build: `npm run build`
- Instalace PHP: `composer require <balíček>`
- Instalace JS: `npm install <balíček>`

### Git a commity
- Trunk-based: hlavní větev `main`; práce na `dev` nebo feature branch
- Nikdy nepushuj přímo na `main` / `production`
- Před commitem a pushem se zeptej uživatele na potvrzení
- Commit zprávy: anglicky, stručné (`feat:`, `fix:`, `docs:`)

### Produkční mód
Pokud `APP_ENV=production`:
- Žádné destruktivní DB operace bez explicitního souhlasu
- Migrace jen s potvrzením
- Vždy upozorni, že běží produkce

### Knihovny a verze
- Nejnovější stabilní verze; ověř na Packagist/npm před instalací
- Nepoužívej deprecated balíčky
- Závislosti (`composer.json` / `package.json`) neměň bez souhlasu

### Testy
- Ke každé nové funkčnosti piš testy (PHPUnit teď; Pest možný později)
- Tenant izolace: CI musí ověřit, že tenant A nevidí data tenanta B
- E2E (Playwright) — zavést od prvních UI flow; konfig v `e2e/` až vznikne
- Před commitem ověř relevantní testy

### Modulární architektura (závazné)
- Každá funkční oblast = **modul**. Jádro: tenancy, users/roles, module system, sdílené služby, routing.
- Test: *„Šel by modul vypnout, aniž by spadl zbytek?"* Komunikace přes kontrakty/eventy.
- Detail: specifikace §3.2 a kap. 5.

### Storefront = Blade SSR (závazné, SEO)
Veřejné stránky e-shopu **musí** být renderované serverem. Nikdy ne SPA.
- **Blade SSR:** homepage, výpis kategorie, detail produktu, vyhledávání, statické stránky, blog, sitemap/feedy, chybové stránky
- **Blade SSR + progressive enhancement:** košík a pokladna (bez JS musí projít; ceny počítá server)
- **Vue/Inertia SPA:** pouze admin nájemce, superadmin, onboarding, fakturace — vše `noindex`
- Vue/Alpine na storefrontu jen jako **ostrůvky** nad hotovým HTML (varianty, galerie, mini-košík, našeptávač, widget Zásilkovny)

Detail a checklist: [`.claude/rules/storefront-rendering.md`](.claude/rules/storefront-rendering.md)

### Multi-tenancy
- Identita tenanta z Host hlavičky (`nazev.droidshop.cz` — finální doména dle deploye)
- Globální scope na modelech (`BelongsToTenant`); `tenant_id` ve všech doménových tabulkách
- Žádné „nahé" DB dotazy mimo Eloquent bez review

### Přístupnost
Standard: **WCAG 2.2 AA** (EAA).
- Skill: [`.claude/skills/accessibility/SKILL.md`](.claude/skills/accessibility/SKILL.md)
- Agent: [`.claude/agents/a11y-checker.md`](.claude/agents/a11y-checker.md)

### Mazací akce
- Všechny mazací akce musí mít potvrzovací dialog

### Bezpečnost
- U každé funkce: auth, validace, SQLi, XSS, CSRF, tenant izolace
- Rizika zapisuj do `security_warnings.md` v rootu
- Při nejistotě upozorni uživatele

### Grafika a UI
- Brand barvy a fonty: doplnit (zatím neutrální Tailwind; vyhnout se typickým AI fialovým/cream šablonám)
- Světlý režim primárně; dark dle potřeby
- Kontrast WCAG 2.2 AA

### Vyhledávání
- Ověřuj aktuální verze a best practices na internetu

## Dokumentace
Rozcestník: [`docs/README.md`](docs/README.md).

| Vrstva | Složka | Formát |
|--------|--------|--------|
| Produktová spec (Level 3) | `docs/specs/` | dlouhodobý dokument |
| Spec (zadání vlny) | `docs/superpowers/specs/` | `YYYY-MM-DD-nazev.md` |
| Plán | `docs/superpowers/plans/` | `YYYY-MM-DD-nazev.md` |
| Chyby | `docs/superpowers/errors/` | `YYYY-MM-DD-error-cislo-nazev.md` |
| As-is | `docs/as-is/` | `YYYY-MM-DD-nazev.md` |

Workflow: **Explore → Plan → Validate → Implement** ([`.claude/rules/structured-workflow.md`](.claude/rules/structured-workflow.md)).
Po milestone: [`docs/as-is/`](docs/as-is/) ([`.claude/rules/as-is-on-milestone.md`](.claude/rules/as-is-on-milestone.md)).

## Struktura projektu

```
/
├── app/                      # Laravel app (jádro + později Modules/)
├── resources/js/             # Inertia/Vue (admin)
├── resources/views/          # Blade (storefront cíl)
├── routes/
├── database/
├── docs/
│   ├── specs/                # Produktová specifikace platformy
│   ├── superpowers/          # specs / plans / errors
│   ├── as-is/
│   ├── design-droidshop/     # Design handoff (zatím prázdné)
│   ├── future/
│   └── legal/
├── .claude/                  # rules, agents, skills
├── .agents/skills/           # Cursor / agent skills (caveman…)
├── .cursor/rules/
├── VERSION
└── CHANGELOG.md
```

## Omezení agenta
- Při nové funkci se zeptej na role / viditelnost
- Před commitem/pushem vždy požádej o potvrzení
- Netriviální úkoly bez schváleného plánu neimplementovat

## Nuance projektu
- **My ≠ prodávající na storefrontu** — nájemce je provozovatel e-shopu; zakotvit ve VOP (spec kap. 11).
- MVP: jedna šablona storefrontu; šablona = modul (rozšiřitelné).
- Vlastní domény + SSL = **hotovo (vlna 2.1)**; víc domén/tenant, DNS-01, vlastní e-mail doména = dál.
- Licence / digitální produkty s aktivačním API = premium, fáze 2 (spec kap. 17).
- Stojí jádro (tenancy, moduly, kernel služby, superadmin), moduly `categories` + `products` + `shipping` + `customers` + `checkout` + `orders` + `payments` + `docs` a veřejný storefront katalogu (Blade SSR, SEO výstupy, 301/410). Zákazník projde nákup od katalogu po děkovnou stránku bez JS včetně platby kartou přes Comgate a dostane fakturu (ruční i automatické vystavení, PDF, e-mail, stažení v účtu); nájemce vidí, edituje a stornuje objednávky v adminu. Faktury, dobropis, proforma a CSV VAT export hotové (vlna 1.6). Self-service onboarding (registrace → wizard → e-shop na subdoméně do 10 minut, cross-host signed auto-login), trial lifecycle scheduler (`billing:sweep-lifecycle`, config `trial_days`/`grace_days`, trial→past_due→suspended), platformní fakturační ledger (netenantový), fakturační profil nájemce hotové (vlna 1.7). **Vlna 1.8 uzavřena:** reálné inkaso platformního předplatného přes Stripe Billing (hostovaný Checkout + Billing Portal, žádné karetní údaje u nás), webhook-driven aktivace (`invoice.paid`/`invoice.payment_failed`/`customer.subscription.deleted`), synchronní `SubscriptionActivator` retirovaný, superadmin manuální aktivace retirovaná (read-only stav). **Vlna 1.9 uzavřena:** deferred billing — roční interval + upgrade/downgrade tarifu base↔premium přes Stripe Billing Portal. Ceny v netenantové `plan_prices` (plan×interval), výběr intervalu v checkoutu, změna tarifu přes Portal + webhook `customer.subscription.updated` → `TenantPlanSwitcher` (rekonciliace modulů), idempotence českého dokladu **per Stripe invoice id** (proration i roční dostane doklad), `invoice.paid` bere částku i tarif z faktury (ne z `tenant->plan`). **Vlna 2.1 uzavřena:** vlastní domény nájemců + automatické TLS (Caddy on-demand). `DomainVerifier` ověří vlastnictví (TXT challenge token + routing CNAME→edge/A→server_ip), jediná autorita nad `verified_at`; `DomainTenantFinder` neresolvuje neověřenou custom doménu; ask endpoint `/internal/tls-check` (shared-secret token + verified+Custom+`allowsStorefront()`) autorizuje emisi; `DomainCertProbe` (`/up` 200 → issued) atomicky s `CanonicalDomain::promote` (custom→primary); `RedirectToCanonicalHost` 301 subdoména→custom; `domains:sweep-pending` hodinově; admin obrazovka `/admin/nastaveni/domena`. Infra (Caddyfile on-demand + deny `/internal/*`, edge DNS, `PLATFORM_TLS_CHECK_TOKEN`) = deploy runbook v as-is. **Vlna 2.2 uzavřena:** šablona storefrontu + branding nájemce. Jedna čistá šablona (Blade SSR, bez JS), přebarvení všech storefront stránek; `tenant_theme` (1:1, logo/favicon/barvy) → CSS proměnné injektované do `<head>` přes composer (cache-safe, per-tenant); `ThemeResolver::sanitizeHex` (`/D` regex) odmítne ne-hex barvu před vstupem do inline `<style>` (CSS-injection guard); `Contrast` helper (WCAG luminance) dopočítá čitelný text; admin `/admin/nastaveni/vzhled` (barvy + upload loga/favicon, favicon jen `png,ico` — SVG je stored-XSS). **Vlna 2.3 uzavřena:** bloková homepage (page builder). Nájemce skládá homepage z 5 typů bloků (`hero`, `product_row`, `category_grid`, `text`, `banner`); relační `homepage_blocks` (modul storefront), řazení `position` + tlačítka (WCAG); storefront render čistý Blade SSR (`HomeController` iteruje visible bloky, partial per typ, vynechá bloky vypnutých modulů); editor Inertia `/admin/m/storefront/homepage` (`admin.storefront.homepage.*`, permission `storefront.homepage.manage`); seed výchozí sady `DefaultHomepage` (provisioner + backfill migrace); text HTML sanitizován při zápisu, obrázky raster-only, `BlockUrl` guard, `image_path` server-authoritative. Další šablony (marketingová/technická) = `docs/future/`. **Vlna 2.4 uzavřena:** varianty produktu (víceosá matice, Velikost × Barva). Čtyři relační tabulky v modulu `products` (`product_options`, `product_option_values`, `product_variants`, `product_variant_values`); kernel `CatalogVariant` kontrakt + rozšíření `ProductCatalog`/`CatalogProduct` o volitelný `?int $variantId` (žádný existující callsite se nemění); server-authoritative resoluce (`option_value_id[]` → `resolveVariant()`, nikdy `variant_id` z klienta); storefront picker radio/select renderovaný Blade SSR bez JS, vanilla JS ostrůvek (`resources/js/storefront.js`) jen pro živý přepočet ceny (vč. „bez DPH") a dostupnosti; košík (`cart_items.variant_id` NOT NULL + sentinel `0`) a objednávka (`order_items.variant_id`/`variant_label` nullable snapshot) nesou variantu; sklad se odepisuje z varianty atomicky ve stejné transakci jako objednávka; admin tab „Varianty" na detailu produktu (osy, hodnoty, generování kartézského součinu, mřížka); globální default zobrazení na `tenant_theme.variant_display`, přepis per produkt. Obrázky/URL/hmotnost per varianta a fasetový filtr katalogu = `docs/future/varianty-obrazky-a-url.md`. **Vlna 2.5 uzavřena:** Zásilkovna. Jádrové kontrakty `Carrier`/`CarrierRegistry`/`PickupPointCatalog`/`ShipmentBook` s guest-safe null bindingy (vzor `PaymentGatewayRegistry` z 1.4); nový modul `packeta` (REST/XML přes `Http` fasádu, žádný SOAP, žádný balíček); netenantový katalog `pickup_points` + denní `packeta:sync-points` s guardem proti prázdnému feedu; server-rendered výběr místa bez JS jako primární cesta, widget jen ostrůvek načítaný na kliknutí; `carts.pickup_point_code` + snapshot místa v objednávce (server-authoritative, klient posílá jen kód); `shipments` s idempotentním podáním (CAS claim + staleness reclaim + fail-fast guard na vztah timeout/práh); hromadné podání, štítky streamované bez ukládání, expediční fronta, blok Doprava v detailu objednávky, tracking v účtu zákazníka; `shipping_methods.settings` nově šifrované. AK §16.5 nesplněné, další dopravci = `docs/future/zasilkovna-dalsi-dopravci.md`. **Vlna 2.6 uzavřena:** slevový engine (kupóny + automatická pravidla). Jádrový kontrakt `DiscountEngine` s guest-safe null bindingy (vzor `PaymentGatewayRegistry`/`CarrierRegistry`); premium modul `discounts` (klíč se odchyluje od spec `coupons`, obsluhuje i pravidla bez kódu); `DiscountAllocator` rozpouští slevu do řádků košíku i objednávky s haléřovou přesností a capacity-aware ořezem; typy procento/pevná částka/doprava zdarma s cílem na kategorii/produkty, limit globální i na e-mail, nejvýš jeden kupón + `combinable` automatická pravidla; čerpání limitu v téže transakci jako odpis skladu, vrácení gated na `returnStock`; e-mailem gatovaný kupón, který selže až při odeslání, objednávku odmítne místo tiché plné ceny; objednávka za 0 Kč settluje bez brány; poznámka o slevě na faktuře degraduje bez jmen slev, když se snímek rozejde se živým součtem; admin `/admin/m/discounts` s ohraničeným hledáčkem produktů. **Vlna 2.7 uzavřena:** akční ceny produktu + zákonná evidence nejnižší ceny za 30 dní (§ 12a zák. 634/1992 Sb., Omnibus). `products.sale_price` + okno kampaně (`sale_starts_at`/`sale_ends_at`, okno **jen na produktu**, částky per varianta), `product_variants.sale_price` s dědičným pravidlem, mrtvý `compare_at_price` zahozen; `ProductCatalog::price()`/`catalogPrice()` vracejí **efektivní** cenu, takže košík, objednávka, doklady i slevový engine 2.6 účtují akční cenu bez změny volajícího kódu (kupón se počítá z akční, ne nominální ceny); řazení katalogu přes `CASE WHEN` nad efektivní cenou; časová řada `product_price_history` zapisuje i **plánované budoucí** intervaly, takže konec akce nepotřebuje cron; `LowestPriceCalculator` počítá okno **ke startu kampaně** (akce není součástí své vlastní reference), badge `−N %` z této reference; storefront Blade SSR bez JS. Zároveň uzavřen nesený dluh 2.6: `tax_rate_id` u dopravy/platby je povinné pro plátce DPH + backfill. Řádek nejnižší ceny ve výpisu kategorie, Omnibus u automatických pravidel, hromadné akce a filtr „ve slevě" = `docs/future/slevy-dalsi-kroky.md`. **Vlna 2.8 uzavřena:** CSV import a export produktů. `ProductCsvSchema` je jediná pravda o formátu pro oba směry (round-trip hlídá test), import zakládá i aktualizuje **podle SKU**, umí varianty (nový `VariantWriter::upsertVariant`), zařazuje do **existujících** kategorií a respektuje limit tarifu; zápis jde výhradně přes `ProductWriter`/`VariantWriter`, takže platí sanitizace, unikátní slug, 301 i zápis do historie ceny z 2.7. Běh je queued job nad `product_imports` (jedna transakce na řádek, chybné řádky do protokolu, přepínač „jen zkontrolovat"); export streamovaný, nákupní cena jen s `products.costs`. Obrázky z URL, mapování sloupců, plánovaný feed a mazání importem = `docs/future/csv-import-dalsi-kroky.md`. **Vlna 2.9 uzavřena:** XML feedy pro Heureku a Zboží.cz. Nový modul `feeds` (**base**), `/feed/heureka.xml` a `/feed/zbozi.xml` na doméně nájemce, XML na request + cache 1 h; vypnutý feed i vypnutý modul vracejí **404**. Varianta = samostatný `SHOPITEM` se sdíleným `ITEMGROUP_ID`, cena vždy z `catalogPrice()` (včetně akce z 2.7), do feedu jen `published()` produkty; `CATEGORYTEXT` z vlastní tabulky mapování s fallbackem na strom nájemce; blok `DELIVERY` z `ShippingOptions`. Admin `/admin/m/feeds` invaliduje cache. Google Merchant, Glami/Favi, import číselníků a per-product opt-out = `docs/future/feedy-dalsi-kroky.md`. **Vlna 2.10 uzavřena:** nastavení modulů (nájemce) + složení tarifů (superadmin). `SettingsService::set()` dosud nikdo nevolal — sedm nastavení modulu `docs` šlo měnit jen v kódu; nově `SettingsSchema`/`SettingsField` nad manifestovým schématem (starý tvar se dál parsuje, pole bez `rules` shodí `modules:sync`), `setMany()` all-or-nothing, `all()` slévá defaulty. Jedna generická obrazovka `/admin/nastaveni/moduly/{modul}` gatovaná manifestovým `settings_permission` (404 na neběžící modul, 403 bez práva). Nové nastavení: `products.variant_display` (přesun z `tenant_theme` + drop sloupce), `checkout.min_order_total`/`guest_checkout` (server-side v `details()` i `place()`), `orders.number_prefix`. Superadmin `/superadmin/tarify` + `PlanModuleReconciler` (rekonciliace živě zapnuté sady všech tenantů tarifu, audit per tenant). **Vlna 3.0 uzavřena:** page cache storefrontu (§15.6), etapa 1 (aplikační vrstva). Jádrová infrastruktura `app/Core/PageCache/` (ne modul), opt-in middleware `page-cache:{dimenze}` za `StartSession`, invalidace generačními čítači ve sloupcích `tenants` (ne cache store, ne tagy) přes observery modelů, CSRF token bezpečně vyměněný za značku a vrácený čerstvý při servírování, přihlášený zákazník cache vždy obchází; sjednocuje pod sebe cache sitemap a feedů z 2.9. Etapa 2 (statický soubor) a etapa 3 (data/fragment cache) čekají na nasazení. **Vlna 3.1 uzavřena:** čtyři nesené technické dluhy. `TenantContext::set()` je jediná cesta přepnutí nájemce (`runAs()`/`runWithoutTenant()` už nevolají `makeCurrent()` napřímo); `ShippingMethod` invaliduje `Dimension::Catalog`, takže přejmenovaný dopravce nezůstane ve feedu hodinu; změna stavu e-shopu píše vlastníkovi přes doménový event z `changeStatus()` (mapa čte **oba** konce přechodu, sweeper své odeslání ztratil, dispatch odložený na commit); statické stránky přesunuty na `/{page-slug}` přes `Route::fallback()` se 301 ze staré cesty. **Vlna 3.2 uzavřena:** právní minimum platformy. Čtyři dokumenty v `docs/legal/` a renderované na `/pravni/*` (VOP, zásady zpracování = my správce vůči nájemci, DPA čl. 28 = my zpracovatel vůči jeho zákazníkům, cookies); souhlas při registraci s datem i verzí; vzory právních stránek nájemce místo prázdna; **editor stránek** (modul `pages` byl read-only, takže vzory by nešlo doplnit ani publikovat); odkazy v patičce a v pokladně. Drafty bez právní revize (rozhodnutí vlastníka), markery `K PRÁVNÍ REVIZI`.

**Vlna hledání (fulltext) zrušena po měření:** `LIKE '%term%'` nad `products.search_text` běží 3,2 ms na base tarifu a 31 ms na stropu premium (5000 produktů), protože `tenant_id` scope zúží scan na katalog jednoho nájemce, a výsledek se navíc od 3.0 cachuje. Přepis na FULLTEXT by přinesl regresi kvality (celá slova místo substringu, `innodb_ft_min_token_size` 3 znaky vyřadí „TV") za úsporu, kterou nikdo nepozná. Vrátit se k tomu, až bude potřeba tarif nad 5000 produktů.

**Vlna 3.3 uzavřena:** cookie lišta + měřicí kódy nájemce. Souhlas se třemi kategoriemi v jádře (`app/Core/Consent/`), nový base modul `analytics` (GA4 s Consent Mode v2, Sklik, Meta Pixel, Heureka Ověřeno zákazníky); server renderuje konfiguraci, nikdy rozhodnutí, takže page cache z 3.0 zůstává nedotčená; GA4 se nenačítá s „denied"; `SettingsField` umí `secret`. **Chování JS neověřuje automatický test** — ověřeno ručně, čeká na Playwright.

**Vlna 3.4 uzavřena:** E2E testy v prohlížeči. 22 testů v `e2e/` (`npm run e2e`, vlastní CI job): souhlas a měření (zavírá mezeru z 3.3), nákup **bez JS** (AK §16.3 poprvé skutečně proklikané), nákup s JS včetně ostrůvku variant a konverze, axe nad 7 stránkami bez blokujících nálezů. Sada jede na `obchod.droidshop` — žádná zvláštní doména ani certifikát nebyly potřeba, obě premisy vlny se ukázaly mylné.

**Vlna 3.5 uzavřena:** layout administrace. Plná šířka, levé menu ve čtyřech kategoriích (kategorie a pořadí drží jádro, manifest říká jen `group`), sbalitelné do ikon, na mobilu drawer; `/admin` má poprvé vlastní **nástěnku**; superadmin dostal totéž rozvržení, ale nechal si tmavý panel. Ikony Lucide (schváleno).

**Zbývá nasazení na VPS.** Vývoj na lokále je hotový: fáze 0–2 kompletní, page cache, právní minimum, měření, E2E i administrace. Otevřené jsou jen věci vázané na server (page cache etapa 2, Caddy TLS, cron, reálné účty Stripe/Comgate/Packeta/GA4) a **právní revize draftů**.

## Rozhodnutí
- 2026-07-17: Produktová spec v1.1 (draft) — multi-tenant SaaS, modularita, shared DB + `tenant_id`
- 2026-07-19: AI workflow z `claude-laravel-vue` + struktura dokumentace ve stylu WooShop
- 2026-07-19: Aktuální kód = Laravel 13 skeleton (Breeze/Inertia); tenancy a moduly teprve
- 2026-07-19: **Storefront povinně Blade SSR** — SEO a marketing nájemce je produktová hodnota; SPA jen admin (viz `.claude/rules/storefront-rendering.md`)
- 2026-07-19: Košík a pokladna také Blade SSR + ostrůvky (ne SPA) — drží AK „checkout funkční bez JS", cenová logika jen na serveru
- 2026-07-19: **URL produktu ploché `/produkt/{slug}`** (odchylka od spec §16.2) — URL se nemění při reorganizaci katalogu; kategorie `/kategorie/{slug}`
- 2026-07-19: `stripe/stripe-php` = zbytek po šabloně, odstranit při první implementační vlně; brána se rozhodne u modulu `billing`
- 2026-07-19: **PHP 8.3** zatím stačí (Laravel 13 vyžaduje `^8.3`, tj. 8.4 je volitelná). Pokud narazíme na funkci vyžadující 8.4 (property hooks, lazy objects, `array_find`), upozorni uživatele — zvedneme constraint na `^8.4`
- 2026-07-19: Page cache — zrušena cookie `has_cart`; mini-košík je ostrůvek, cachované HTML nesmí obsahovat osobní obsah (spec §15.6)
- 2026-07-19: **Multi-tenancy = `spatie/laravel-multitenancy` ^4.1** — navržený pro shared DB + `tenant_id`. `stancl/tenancy` zamítnut (těžiště v DB-per-tenant). Kernel služby dle spec §15.1 píšeme sami
- 2026-07-19: **Moduly = `nwidart/laravel-modules` ^13.0 + vlastní manifest vrstva** — nwidart dá autoloading, scaffolding, migrace per modul; naše vrstva manifest schema, per-tenant aktivaci, route mounting, kill switch, tarify. Kompromis: `modules_statuses.json` = deploy stav, tabulka `tenant_modules` = per-tenant stav. Revidovatelné (alternativa = plně vlastní systém)
- 2026-07-19: **Fáze 0 rozdělena na vlny.** Vlna 0.1 = tenancy jádro + izolace + CI (bez modulů, bez superadmin UI)
- 2026-07-19: **Úložiště = lokální disk pro MVP**, ne S3 (změna původního rozhodnutí „S3 od začátku"). Soubory zůstávají na naší VPS. `FileStorage` služba jádra drží abstrakci — modul nikdy nesahá na disk přímo, takže přechod na S3 je pak jen změna configu. Háček: lokální disk váže na jeden server (víc app serverů = nutné S3) a soubory musí být v záloze VPS. Platí, dokud běžíme na jedné VPS.
- 2026-07-20: **Admin routy modulů jsou za `module:{key}` → `tenant.member`.** Laravelí alias `auth` se schválně nepoužívá — sedí v middleware priority listu a byl by přeřazen před modulový gate, čímž by z jeho 404 udělal redirect na login a prozradil, které moduly e-shop provozuje. Autentizaci dělá `EnsureTenantMember` sám (vyhodí `AuthenticationException`)
- 2026-07-20: **Oprávnění se odvozují z manifestů modulů, které tenant běží** (`TenantPermissions` + `Gate::before`). Právo vypnutého modulu nedostane nikdo, ani vlastník — jinak by deaktivace modulu nechala jeho autorizační plochu otevřenou
- 2026-07-20: **Převody DPH sedí na `TaxRate`, ne na `Money`** (odchylka od plánu vlny 1.1). `Money` je nejprimitivnější hodnotový typ jádra a nesmí znát daň; závislost míří jen jedním směrem
- 2026-07-20: **Inertia stránky modulů leží v `resources/js/Pages/Modules/<Modul>/`**, ne uvnitř modulu. Inertia view finder skládá cestu `{page_path}/{component}.vue`, takže krátký název na cestu uvnitř modulu nenamapuješ bez vlastního finderu. Blade views, routy, controllery a migrace v modulu zůstávají
- 2026-07-20: **Sanitizace tenantem psaného HTML vlastní** (`app/Core/Html/HtmlSanitizer.php` nad `DOMDocument`), bez `htmlpurifier`. Čistí se **při zápisu**, ne při renderu — jinak by se politika rozhodovala znovu na každém call site
- 2026-07-20: **Drag&drop nikdy jako jediná cesta.** Řazení kategorií i obrázků má tlačítka ovladatelná klávesnicí (WCAG 2.1.1). Tažení lze doplnit jako nadstavbu
- 2026-07-20: **Šablona storefrontu = modul `storefront` (core), ale bez `requires` na katalog.** Core modul nejde vypnout a nic pod ním taky ne, takže deklarovaná závislost by z `products` udělala nevypnutelný modul. Šablona se ptá za běhu (`ShopModules`) a vykreslí, co e-shop běží
- 2026-07-20: **Kořenová routa `/` zůstává v jádře a deleguje přes kontrakt `StorefrontHome`.** Core web routy se matchují dřív než modulové, takže modulová routa pro `/` by se nikdy netrefila. Jádro se implementace ptá na modulový klíč, takže kill switch i per-tenant aktivace platí dál
- 2026-07-20: **Redirecty se servírují z handleru `NotFoundHttpException`, ne middlewarem.** Middleware by stál DB dotaz na každém zobrazení produktu; redirect má smysl jen tam, kde už žádná routa nesedí. Responder si přitom sám dohledá tenanta z hostu — nenamatchovaná cesta neprojde `web` skupinou, takže tenant kontext v ní ještě není nastavený
- 2026-07-20: **Vyhledávání přes normalizovaný sloupec `products.search_text` + `LIKE`, ne InnoDB fulltext** (odchylka od §16.1). Fulltext neumí české skloňování ani diakritiku a nejede na SQLite v testech. Normalizace při zápisu je stejně povinná podle §4.1. Háček: `LIKE '%term%'` nepoužije index — u velkých katalogů se bude přepisovat
- 2026-07-19: **Redis je vědomá závislost, ne pohodlí.** Session/fronty/základní cache jdou přepnout na `database` driver bez zásahu do kódu a izolace tenantů zůstane (prefix cache funguje na každém storu). Ale **tagy umí jen Redis** (`database` a `file` je neumí — ověřeno), takže invalidace page cache dle §15.6 by se bez Redisu musela přepsat. Pokud hosting Redis nemá, je to rozhodnutí do specifikace, ne tichý fallback. Navíc: absence Redisu obvykle značí sdílený hosting, kde nejde držet `queue:work` démona — a to bolí víc než cache.
- 2026-07-20: **Identita odesílatele e-mailu sedí na `tenants`, ne v `settings`.** `SettingsService` validuje proti manifestu modulu a mail je jádrová služba bez modulu — průchod přes settings by znamenal vymyslet falešný modul. Obálková adresa zůstává vždy naše (SPF/DKIM), tenant dostává jen display name a reply-to
- 2026-07-20: **Limit `emails_month` nikdy nezastaví transakční poštu.** `MailKind` je povinný argument kontraktu (`Transactional` / `Bulk`), bez výchozí hodnoty — aby se do špatného chování nedalo dojít opomenutím. Vyčerpaný tarif nesmí utnout potvrzení objednávky ani reset hesla: nájemcův nedoplatek by pak odnášel jeho zákazník, který se nedostane do účtu a nedozví se, že má zaplatit. Transakční zprávy se počítají, jen neblokují. Počítadlo bere `queued` i `sent` — čtení jen odeslaných šlo obejít dávkou, která se do fronty vejde celá dřív, než worker doručí první kus
- 2026-07-20: **Finální selhání e-mailu hlásí hook `failed()`, ne počítadlo pokusů.** `SyncJob::attempts()` vrací natvrdo 1, takže podmínka na poslední pokus by na sync driveru nikdy nesepnula a zpráva by navždy zůstala ve stavu `queued`. Během opakování se ukládá jen text chyby, stav se nemění
- 2026-07-20: **Reset hesla zákazníka jede na vlastních tokenech, ne na Laravelím brokeru hesel.** `password_reset_tokens` má primární klíč `email` a repozitář hledá token jen podle adresy. `customers.email` je ale unikátní jen v rámci tenanta, takže zákazníci dvou e-shopů se stejnou adresou by si tokeny tiše přepisovali napříč tenanty — cizí vyžádání resetu by zneplatnilo odkaz někoho jiného. Modul `customers` proto drží vlastní `CustomerTokens` nad tabulkou `customer_tokens` klíčovanou `(tenant_id, email, purpose)`, ukládá jen hash tokenu a stejný mechanismus obsluhuje i verifikaci e-mailu
- 2026-07-20: **GDPR výmaz zákazníka anonymizuje řádek, nemaže ho.** Modul `orders` bude na zákazníka odkazovat cizím klíčem — smazání řádku by buď spadlo na omezení klíče, nebo nechalo viset odkaz a z žádosti o výmaz udělalo rozbitou historii objednávek. `CustomerEraser` přepíše jméno, e-mail (na neuhodnutelný placeholder v doméně `anonymized.invalid`), telefon a adresy, zahodí heslo a orazítkuje `anonymised_at`; běží transakčně, je idempotentní a zapisuje do `AuditLog`
- 2026-07-20: **Anonymizovaného zákazníka odmítá guard, ne middleware ani globální scope na modelu.** `AnonymisedCustomerProvider` (driver `customer-eloquent`) filtruje `whereNull('anonymised_at')` ve všech třech cestách, kterými guard `customer` dohledává uživatele — session, remember-me, přihlášení. Admin dál vidí anonymizované zákazníky beze změny, protože filtr sedí jen na autentizační cestě, ne na modelu. Efekt: zákazník smazaný uprostřed živé session dostane při dalším requestu z guardu `null` a `auth:customer` ho vyhodnotí jako hosta — bez `AuthenticateSession` middlewaru nebo ručního odhlašování
- 2026-07-21: **Prázdná matice doprava×platba znamená „všechny platby povoleny", ne žádná.** Pivot `shipping_method_payment_method` zapisuje omezení, ne povolení. Nájemce, který matici nikdy neotevře, musí mít funkční checkout — doprava bez řádků v pivotu proto nabídne všechny aktivní platby. Opačná volba (prázdná = nic) by z nedotčené obrazovky udělala e-shop, který nepřijme objednávku
- 2026-07-21: **Platební nastavení s tajemstvím je šifrované (`encrypted:array`), dopravní ne.** Výdejní adresa a otevírací doba se tisknou na storefrontu — nejsou tajné a zůstávají prostým JSONem. Bankovní účet pro QR je credential podle §16.5: `payment_methods.settings` je šifrované, v adminu maskované, mění se opětovným zadáním. První tenant-scoped použití `encrypted` castu (dosud jen `PlatformAdmin`)
- 2026-07-21: **Odebrání položky z košíku se maže hned, bez potvrzovacího dialogu.** Pravidlo „všechny mazací akce mají potvrzovací dialog" míří na destruktivní perzistentní akce (smazání produktu, storno objednávky, GDPR výmaz) — ne na reverzibilní obsah košíku. Košík je přechodný stav, který zákazník sám plní a odebírá; mezistránka na potvrzení odebrání položky (bez JS by musela být plnohodnotná GET stránka jako `address-delete`) je proti standardnímu e-shop UX a škodí konverzi. Storno a mazání v adminu potvrzení mají dál.
- 2026-07-21: **QR platby jedou na `endroid/qr-code:^6.0`, ne `^6.1`** (odchylka od plánu vlny 1.3). `^6.1` vyžaduje PHP 8.4, projekt drží PHP 8.3 (rozhodnutí „PHP 8.3 zatím stačí"). `^6.0` (6.0.9) má stejný v6 `SvgWriter` bez GD, běží na PHP `^8.2`, SPAYD QR se kreslí jako inline SVG. Až projekt zvedne constraint na PHP 8.4, lze bumpnout na `^6.1`.
- 2026-07-21: **`checkout` nedeklaruje `requires` na `orders` ani `shipping`.** Objednávku zakládá přes `app(OrderPlacement::class)` (jádrový null binding ji odmítne, pokud `orders` neběží) a dopravu/platbu přes `ShippingOptions`/`PaymentOptions` (null → vestavěná nouzovka „osobní odběr zdarma"). Runtime gate přes `ShopModules`, ne manifestová závislost — stejný precedent jako `CustomerIdentity` v etapě 2. Deklarovaná závislost by ze `orders`/`shipping` udělala nevypnutelné moduly, jakmile by je e-shop jednou zapnul spolu s checkoutem
- 2026-07-21: **Cenová autorita je vždy `ProductCatalog::price()`, `cart_items.unit_price` je jen zobrazovací snímek.** `OrderPlacer` každý řádek při odeslání přepočítá z katalogu a nikdy nedůvěřuje snímkované ceně v košíku ani ničemu z POST dat. Neshoda snímku se skutečnou cenou vyhodí `PriceChanged` (banner + přepočet), místo tichého naúčtování staré částky
- 2026-07-21: **Odpis skladu běží uvnitř téže transakce jako zápis objednávky.** `OrderPlacer::place()` volá `ProductCatalog::decrementStock()` (atomický `UPDATE`) dřív, než vloží řádek do `orders` — objednávka, která nevezme sklad, nesmí vzniknout, a naopak sklad vzatý bez uložené objednávky se musí vrátit. Souběh na posledním kusu prohraje na `UniqueConstraintViolationException`, ne na chybějícím skladu: `order_idem_unique` rozhodne, který požadavek dostane existující objednávku místo 500
- 2026-07-21: **Dvojitý stavový automat (`fulfillment_status` × `payment_status`) vynucuje service `OrderWorkflow`, ne UI.** Nezávislé grafy přechodů, nezávislé `order_events` záznamy — objednávka označená „zaplaceno" neříká nic o tom, jestli je zabalená, a naopak (dobírka je běžný případ, kdy „odesláno" nastane dávno před „zaplaceno"). Kontrola nelegálního přechodu proběhne čistě v paměti (lookup do pole) před jakýmkoli dotazem, takže odmítnutý přechod nemá co vracet
- 2026-07-21: **`CatalogProduct` rozšířen o `catalogTaxRatePercent()`.** Snímek řádku objednávky (`order_items.tax_rate`) i VAT rekapitulace v košíku (`CartPricer`) potřebují sazbu DPH produktu v okamžiku nákupu z katalogu, ne přes samostatný dotaz na `TaxRate` — stejná pravda pro cenu i sazbu z jednoho místa
- 2026-07-21: **Kernel drží tvary (`CartShape`/`PlacedOrder`/`OrderView`), moduly je implementují.** Stejný vzor jako `CatalogProduct` u `products` — `checkout` a `orders` vystavují svým modelům (`Cart`, `Order`) tyto shapes přímo, takže volající mimo modul (druhý modul, budoucí `payments`, účet zákazníka) nikdy nesahá na Eloquent model cizího modulu, jen na kontrakt v `app/Core/`
- 2026-07-21: **Platební brány = registry/driver od začátku, ne jeden binding** (vlna 1.4). `PaymentGatewayRegistry::for($provider)` v `app/Core/Payments/` vrací driver podle klíče `payment_methods.provider`; víc bran koexistuje per tenant. Vlna 1.4 registruje jediný driver `ComgateGateway`; GoPay/Stripe = pozdější drivery bez zásahu do checkoutu/webhooku. `NullPaymentGatewayRegistry` = guest-safe (vypnutý modul → `for()` null, `available()` prázdné → online platba se vůbec nenabídne, pokladna jede na offline). Stripe v repu (`stripe/stripe-php`) je pro billing platformy, nemíchat se storefront platbami tenantů
- 2026-07-21: **Verify-before-trust je jediná autorita o zaplacení.** `payment_status = paid` se nastaví výhradně po server-to-server `PaymentGateway::verify()` dotazu na status API brány — nikdy z query návratu (`/platba/navrat`) ani z těla webhooku. Podvržený `?status=paid` nezaplatí nic. Navíc kontrola částky: `paid` výsledek s jinou částkou než `orderTotal()` se nesettluje. Referenci brány váže `orders.payment_reference` uložená při `initiate()`, takže callback re-verifikuje TU referenci, ne referenci z requestu — cizí zaplacená transakce nejde napojit na jinou objednávku
- 2026-07-21: **Settlement přes kontrakt `OrderSettlement`, ne saháním do `OrderWorkflow`.** Modul `payments` verifikuje (má driver), ale přechod stavu + návrat skladu dělá modul `orders` přes `App\Core\Orders\Contracts\OrderSettlement` (`attachReference`/`settlePaid`/`settleFailed`), vedle placement logiky, která sklad vzala. Idempotence: `transitionPayment` je no-op při `from==to` (duplicitní webhook + návrat = jedna změna, jeden `order_events`), settlement běží pod `lockForUpdate`. Graf plateb rozšířen o `failed` (`unpaid→{paid,failed}`, `failed→unpaid`)
- 2026-07-21: **Webhook `/platba/notifikace` mimo CSRF, autentizace podpisem brány.** S2S request nemá session/token; route má `withoutMiddleware(VerifyCsrfToken)` a autenticitu ověřuje `PaymentGateway::verifyNotification()` (Comgate: shared secret v těle). Tenant se řeší z hostu (každý nájemce má vlastní Comgate účet nakonfigurovaný na svou doménu) přes `web`+`module:payments` skupinu. Webhook vždy vrací 2xx po zpracování (i „neznámá objednávka") aby Comgate přestal opakovat; 4xx jen na neověřený/malformovaný
- 2026-07-21: **Expirace neuhrazených online objednávek = odložený queue job, ne cron.** `ExpireUnpaidOrder` dispatchne `ComgateGateway::initiate()` s delay (`config('payments.reservation_ttl_minutes')`, default 30); na běhu označí objednávku `failed` a vrátí sklad, ale jen když je stále `unpaid` (jinak no-op). Tenant-aware fronta. **Háček:** na `sync` driveru by odložený job běžel hned, proto se tam neplánuje a expirace = ruční storno (vrací sklad taky). Retry po failed platbě = nová objednávka, ne in-place re-pay — cancelled platba už vrátila sklad, který mohl mezitím zmizet
- 2026-07-21: **Comgate driver přes `Http` fasádu, bez composer balíčku.** E-commerce v1.0 HTTP-POST protokol (`/create`, `/status`, form-encoded); credentials (merchant/secret/test) per-tenant v `payment_methods.settings` (`encrypted:array`, maskované v adminu, keep-on-update jako QR účet), nikdy v `.env`/configu. `GatewayError` je jádrová výjimka (`app/Core/Payments/`), aby ji checkout chytil bez importu modulu
- 2026-07-22: **Modul `docs` je base; čte a píše přes oddělené kontrakty.** `DocumentIssuer` (write: `issue()`, jádrový null binding `NullDocumentIssuer` vyhazuje) a `DocumentBook` (read: `forOrder()`, `NullDocumentBook` vrací prázdno) — stejný read/write split jako `OrderBook`/`OrderPlacement`. Cizí modul nikdy nesahá na model `Document`
- 2026-07-22: **Doklad je immutable snapshot; oprava jen dobropisem.** Model povolí update jen `pdf_path`/`sent_at`, delete vždy vyhodí. Dobropis = vlna 1.6
- 2026-07-22: **Auto-vystavení přes doménový event z `OrderWorkflow`, deferovaný `DB::afterCommit`.** `settlePaid` nestuje `transitionPayment` do vnější transakce, takže inline dispatch by běžel před commitem — `DB::afterCommit` posune vystřel na skutečný commit. Payments/orders nezná modul `docs`
- 2026-07-22: **PDF přes `barryvdh/laravel-dompdf` (^3.1), ne mpdf/Browsershot.** Pure PHP, bez systémové binárky — sedí k local-first VPS. Faktura A4 = tabulkový layout (dompdf nemá flex/grid)
- 2026-07-22: **Neplátce DPH = render distinkce, ne nový typ.** Plátce → „Faktura – daňový doklad" (DIČ, DPH rekapitulace), neplátce → „Faktura" bez DIČ, `vat_summary` nulové. Enum `type` má invoice/proforma/credit_note od začátku, 1.5 vystavuje jen `invoice`
- 2026-07-22: **Snapshot dodavatele z `tenants.billing_*` v okamžiku vystavení.** Pozdější změna profilu tenanta nemění vystavený doklad
- 2026-07-22: **Faktura PDF na privátní disk (`FileStorage` `tenant_private`), stažení jen přes gated route.** Admin: `docs.manage`; zákazník: `auth:customer` + `customer.session` + vlastník objednávky (`OrderBook::findForCustomer`, cizí = 404 bez leaku), `noindex`. Nikdy veřejné URL
- 2026-07-22: **Registry + `DocumentWriter` (vlna 1.6).** `DocumentIssuer` deleguje per typ přes `DocumentIssuerRegistry`; sdílená write mechanika (číslo, immutable insert, idempotence, PDF dispatch, unique-violation fallback) v `DocumentWriter`; per-typ pravidlo v `TypedDocumentIssuer` (`InvoiceIssuer`/`CreditNoteIssuer`/`ProformaIssuer`). Precedent `PaymentGatewayRegistry` (2026-07-21)
- 2026-07-22: **Číslování — series klíč nese rok (vlna 1.6).** `SequenceService` series `invoices:2026` → čítač se každý rok resetuje (nový rok = nový řádek `sequences`, žádná migrace). Číslo `{PREFIX}{YYYY}{NNNN}` skládá nový core `App\Core\Documents\DocumentNumber`; přidán syrový `SequenceService::nextNumber()`, `next()` beze změny zůstává pro `orders` (číslo objednávky)
- 2026-07-22: **Dobropis = jen plný storno, ruční, gated.** Tlačítko v detailu objednávky, žádný automat. Podmínka: faktura existuje **a** objednávka `cancelled` nebo `refunded`, jinak `CreditNoteNotAllowed` → 422 (tlačítko navíc skryté). Snímek = negace faktury (peníze mění znaménko, sazba DPH `rate` zůstává), odkaz na originál přes `corrects_document_id`/`corrects_number`. Vlastní řada `credit_notes`, PDF bez QR (dobropis nežádá platbu)
- 2026-07-22: **Proforma = ruční, nedaňový doklad.** `taxable_at` = null (bez DUZP), patička „Není daňový doklad", vlastní řada `proformas`, QR pro převod (žádost o platbu). Koexistuje s fakturou na jedné objednávce — unique přepsán na `(tenant_id, type, number)`, takže obě řady smí sdílet stejné číslo
- 2026-07-22: **CSV VAT export dle DUZP, jen `invoice`+`credit_note`.** Nový kontrakt `App\Core\Documents\Contracts\DocumentLedger` (`taxableBetween()`), proforma vyloučena (není daňový doklad), dobropis záporně. Streamovaný, UTF-8 BOM, oddělovač `;` (české Excel locale). CSV formula injection (CWE-1236) neutralizována v `VatCsvWriter` — volné textové sloupce (jméno, IČO, DIČ) escapované vedoucí uvozovkou, peněžní sloupce vědomě ne (záporná částka dobropisu by se jinak zalomila jako text a rozbila `SUM()`)
- 2026-07-22: **Schéma `documents` upraveno pro dobropis/proformu.** `total` `UNSIGNED BIGINT`→`BIGINT` (dobropis je záporný), `taxable_at` NOT NULL→nullable (proforma bez DUZP), unique `(tenant_id, number)`→`(tenant_id, type, number)` (číselné řady jsou per typ, prázdné prefixy by jinak kolidovaly na stejném čísle). Migrace jako alter na již nasazenou tabulku z 1.5, ne přepis historie
- 2026-07-22: **Zakládání tenanta = jeden recept `TenantProvisioner` (vlna 1.7).** Transakčně: tenant (`trial`, `trial_ends_at`), primární subdoména (validace formátu + rezervovaných + unikátnost), owner do `tenant_users`, aktivace modulů tarifu, audit. `DemoShopSeeder` volá tuto službu — žádná druhá cesta. Audit `tenant.provisioned` běží v `runAs($tenant)`, protože `AuditLog` bere `tenant_id` z ambient kontextu, který při zakládání ještě není nastavený
- 2026-07-22: **Onboarding = registrace → Inertia wizard → shop; cross-host přechod přes signed URL.** Owner se registruje na platform hostu, admin běží na subdoméně; `SESSION_DOMAIN=null` (host-only cookie) znamená, že platform session na subdoméně neplatí. Přechod řeší krátkodobá signed URL na cílovém hostu (`onboarding.enter`), která tam založí web-guard session — vzor `ImpersonationController::begin`. Autorita: podpis kryje celou absolutní URL (proto `URL::forceRootUrl` na tenant host před podpisem, ne přepis hotové URL), 5 min TTL, membership check (cizí user → 403), `session()->regenerate()`. Schéma vždy `https` (za TLS-terminating proxy by `getScheme()` vrátil `http` a rozbil podpis). Landing `/admin` = jádrová routa `admin.home` → první položka `NavigationBuilder`, fallback `admin.billing.edit`
- 2026-07-22: **Subdoména se validuje server-side vždy; availability endpoint je jen pohodlí.** `SubdomainName::fromInput()` (formát RFC label 3–63, rezervované z configu) je autorita v `CreateShopRequest`; `GET /onboarding/subdomena/check` (`no-store, private`) jen předběžně informuje wizard. Kolizi řeší DB unique + `SubdomainTaken`, ne kontrola-pak-zápis
- 2026-07-22: **Trial lifecycle = denní command `billing:sweep-lifecycle` (`NotTenantAware`).** Config `trial_days=14`/`grace_days=7`; `trial` po expiraci → `past_due` (storefront běží dál, spec odchylka §2), `past_due` po `trial_ends_at + grace_days` → `suspended` (+ e-mail ownerovi). Přechody přes `Tenant::changeStatus` v `runAs($tenant)` (audit tenant_id). `NotTenantAware` je pro Command inertní (hook jede jen na queued job) — držen jako marker pro budoucí převod na job; skutečná ochrana = command nemá ambient tenant a `MailService` dostává `$tenant` explicitně. **Háček 1.8:** `past_due` je kotvený na `trial_ends_at`; až přibude cesta `active→past_due` (zmeškaná platba), bude potřeba vlastní `past_due_at`
- 2026-07-22: **Platformní billing = samostatný netenantový ledger (`app/Core/Billing/`), docs modul se nešahá.** My fakturujeme nájemci za předplatné — jiná kniha než docs (kde nájemce fakturuje svým zákazníkům). `platform_invoices`/`platform_sequences` bez `tenant_id` (odběratel = `billed_tenant_id`, ne scope), allowlistované v `SchemaConventionTest`. Vlastní gap-free `PlatformSequenceService` (tenant-scoped `SequenceService` by bez tenant kontextu vyhodil). Číslo `PF{YYYY}{NNNN}` přes sdílený `DocumentNumber`. Dodavatel = `config('billing.company')`, odběratel = snímek `tenants.billing_*`; immutable model (update jen `pdf_path`/`sent_at`). VAT split řídí *náš* `vat_payer` (config), ne nájemcův
- 2026-07-22: **`PlatformInvoiceWriter` idempotentní per období.** Klíč `(billed_tenant_id, period_from, period_to)` (unique index + pre-check + `UniqueConstraintViolationException` catch); alokace čísla + insert v `DB::transaction` (gap-free při selhání); `MissingBillingProfile` (prázdné `billing_name`) padne před alokací čísla. PDF renderuje **po** commitu, best-effort (`try/catch` → `report`, `pdf_path` zůstane null) — queued regen job je follow-up
- 2026-07-22: **Reálné inkaso za předplatné je za kontraktem `SubscriptionGateway` (vlna 1.7 jen `NullSubscriptionGateway`).** `charge()` vrací `ChargeResult`; null driver = dev auto-success bez pohybu peněz. `SubscriptionActivator`: charge → (úspěch) → vystav fakturu → `changeStatus(Active)` → prodluž období; faktura je *důsledek* zúčtování, ne naopak. Superadmin akce „Aktivovat předplatné" guarduje `PendingDeletion`/`Deleted` (nelze) a `Active` (žádná re-aktivace = žádné dvojité inkaso), `changeStatus` sám přechody nevaliduje. Stripe driver = vlna 1.8 (žádný zásah do onboardingu/scheduleru/ledgeru). **Háček 1.8:** activator commituje fakturu ve vlastní transakci a teprve pak mění stav mimo ni — u reálné brány by charge-success-then-issue-fail vzal peníze bez aktivace
- 2026-07-22: **Fakturační profil nájemce = jádrová admin obrazovka `/admin/nastaveni/fakturace` (nová core route skupina `routes/tenant.php` pod `['web','tenant.member']`).** Slouží dvěma pánům: dodavatel na fakturách nájemce (docs snímek) + odběratel na naší platformní faktuře. Bez ní nešel vystavit ani docs doklad (dodavatel byl prázdný). Sdílený Inertia prop `billingProfileComplete` + banner v adminu = cesta k obrazovce (není v modulové nav)
- 2026-07-22: **Vlastní doména nájemce = fáze 2, ne 1.7.** Datový model připraven (`domains.type=custom`, `ssl_status`, `DomainTenantFinder` řeší libovolný host), chybí ověření vlastnictví (DNS TXT/CNAME), automatická emise TLS a stavové UI — netriviální infra na VPS, samostatná vlna
- 2026-07-22: **Platformní předplatné = Stripe Billing, ne PaymentIntents (vlna 1.8).** Stripe řídí opakovaný fakturační cyklus, dunning a SCA/3DS retry; my jen reagujeme webhooky. Karta se sbírá přes hostovaný Stripe Checkout (`subscription` mode), správa (karta, zrušení, historie) přes hostovaný Billing Portal — žádné karetní údaje u nás, PCI SAQ-A. `SubscriptionGateway` přepsán na `startCheckout(Tenant, Plan): string` + `billingPortalUrl(Tenant): string`; synchronní `charge()`/`SubscriptionActivator`/`ChargeResult`/`ChargeFailed` retirovány — `SubscriptionCharge`/`MissingBillingProfile`/`PlatformInvoiceWriter` zůstávají
- 2026-07-22: **Aktivace předplatného je webhook-driven, ne superadmin pull.** Superadmin manuální „Aktivovat předplatné" retirováno (jen read-only stav v detailu tenanta) — self-service nájemce přes Checkout, aktivaci potvrzuje Stripe, ne administrátorský klik. Uzavírá háček 1.7 (charge-success-then-issue-fail): peníze a aktivace teď vždy jdou ve stejném pořadí (zaplaceno → doklad → status), protože obojí spouští jeden webhook handler
- 2026-07-22: **Náš netenantový ledger je autoritativní na `invoice.paid`, ne na Stripe invoice.** Stripe invoice je jen inkasní doklad; český daňový doklad vystavuje `PlatformInvoiceWriter` na `StripeWebhookHandler::onInvoicePaid`, idempotentně per fakturační období (stejný klíč jako 1.7: `billed_tenant_id`+`period_from`+`period_to`) — duplicitní doručení webhooku nevystaví druhou fakturu
- 2026-07-22: **Idempotence webhooku = claim `stripe_events.event_id` atomicky se zpracováním v jedné `DB::transaction`, ne odděleně.** Odděleně (claim-commit-pak-zpracuj) by mid-processing selhání nechalo claim zapsaný a Stripe retry by narazil na unique constraint a event by se ztratil navždy bez efektu. V jedné transakci: duplicitní event ztratí unique insert a celá transakce (včetně claimu) se vrátí; skutečné selhání zpracování vrátí i claim, takže retry projde znovu
- 2026-07-22: **Lifecycle sweeper (`billing:sweep-lifecycle`) přeskakuje tenanty s `stripe_subscription_id`.** Jakmile má tenant Stripe subscription, jeho lifecycle řídí webhook (`invoice.payment_failed`→`past_due`, `customer.subscription.deleted`→`suspended`), ne sweeperův odpočet od `trial_ends_at` — bez guardu by sweeper mohl suspendnout tenanta, kterého Stripe teprve retriuje v rámci vlastní dunning politiky
- 2026-07-22: **Paid-through datum reuse `trial_ends_at`, ne nový sloupec.** `invoice.paid` přepíše `trial_ends_at` na `current_period_end` — stejné pole čte trial banner (1.7), tenant-facing subscription screen i superadmin read-only blok jako „platí do", bez ohledu na to, jestli je tenant ještě v trialu nebo už placený. **Háček:** název pole je matoucí mimo kontext; zvážit vlastní `paid_through_at`, pokud přibude potřeba nezávisle trackovat historii období
- 2026-07-22: **Webhook endpoint `POST /superadmin/stripe/webhook` autentizovaný jen podpisem, ne session.** Bez CSRF (`withoutMiddleware(VerifyCsrfToken)`), bez `auth`, autenticita = `Stripe-Signature` header ověřený `\Stripe\Webhook::constructEvent()` proti signing secretu (vzor Comgate webhook 1.4). Vždy 2xx po úspěšném ověření podpisu (i „neznámý zákazník"), aby Stripe přestal opakovat; 4xx jen na neplatný/chybějící podpis. Cesta v `/superadmin/*`, ne `/platform/*` z návrhu specu — sedí do existující konvence netenantových platformních rout
- 2026-07-22: **`CheckTenantStatus` vynucuje admin write-freeze přes `allowsAdminWrite()`, ne jen read-gate.** Admin routy: `Deleted` → 503 (žádný read); Suspended/PendingDeletion vidí read-only admin (stáhnout/exportovat data, §6.0) ale nebezpečné metody (POST/PATCH/DELETE) → 503, **výjimka `admin.subscription.checkout`/`admin.subscription.portal`** (jinak by se suspendovaný nájemce nemohl zaplatit zpět). Trial/Active/PastDue zapisují normálně (`allowsAdminWrite()` true). Háček z 1.8: read-gate split se nejdřív svezl v nesouvisejícím fix commitu bez write-gate → final review odhalil, že suspendovaný nájemce mohl zapisovat v adminu; write-freeze doplněn. Enforcement je verb-based (`isMethodSafe()`), takže mutující GET (např. dev-only `devComplete`) ho obejde — dořešit, pokud přibude mutující GET v produkci
- 2026-07-23: **Ceny tarifů = netenantová `plan_prices` (plan × interval), ne sloupec na `plans` (vlna 1.9).** Roční interval potřebuje víc Stripe price ids na tarif, takže jeden `plans.stripe_price_id` nestačí — zrušen, data přesunuta do `plan_prices` (`interval` month/year, `stripe_price_id`, `price_amount`). Netenantová jako `plans` (allowlist v `SchemaConventionTest`). `Plan::priceFor(BillingInterval)` mapuje interval na řádek; checkout resolvne price z něj. Mapování zpět (`price.id` → plan+interval) drží webhook `invoice.paid` i `subscription.updated`
- 2026-07-23: **Idempotence platformního dokladu = per Stripe invoice id, ne per období (vlna 1.9, odchylka od 1.7/1.8).** Proration faktura při změně tarifu mid-period má překryv období — per-období klíč `(billed_tenant_id, period_from, period_to)` by ji zahodil a nájemce by nedostal daňový doklad na doúčtovanou částku. Stripe je autorita o počtu a částce faktur, takže `platform_invoices.stripe_invoice_id` (unique) je nový idempotenční klíč; `period_from/to` zůstávají jako data. `SubscriptionCharge` nese `stripeInvoiceId` + `grossTotal` (částka z faktury, ne `plan->price_month`) — jinak by proration/roční dostaly špatnou částku
- 2026-07-23: **Změna tarifu je Portal-driven, my reagujeme na `customer.subscription.updated`.** Stripe Billing Portal nativně umí přepínání cen/intervalů s proraci — nestavíme vlastní upgrade UI ani nevoláme `subscriptions->update` sami. Webhook mapuje nové `price.id` → `plan_prices` → plan+interval a volá `TenantPlanSwitcher::switchTo` (repoint `plan_id`/`billing_interval` + **plná rekonciliace modulů**: aktivuj moduly nového tarifu, deaktivuj old-only). `invoice.paid` derivuje částku (`amount_paid`) i tarif (line `price.id`) z faktury, guard `amount_paid==0` (downgrade kredit) → žádný doklad. Háček: `ModuleRegistry::guardPlan` čte živou relaci `$tenant->plan`, takže switcher po změně `plan_id` musí `unsetRelation('plan')`, jinak guard odmítne aktivaci modulů nového tarifu
- 2026-07-23: **`TenantPlanSwitcher` rekonciluje moduly proti živě zapnuté sadě, ne proti `planChanged` příznaku (vlna 1.9, oprava final review C1).** Stripe negarantuje pořadí webhooků: když `invoice.paid` (nastaví `plan_id`) dorazí před `customer.subscription.updated` (rekonciluje moduly), gate na „změnil se plan_id" by rekonciliaci přeskočil a nájemce by platil premium bez premium modulů. Switcher proto počítá `activate = tarifKlíče − zapnuté`, `deactivate = zapnuté ∩ (katalogVšechTarifů − tarifKlíče)` z aktuálního stavu — idempotentní a nezávislé na pořadí. `deactivate` bere jen klíče z `plan_modules` (core moduly tam nejsou → nikdy se nedeaktivují). `activate` se protíná s `ModuleRegistry::available()`, takže globálně kill-switchnutý modul (chybí v `available()`) se přeskočí místo výjimky `UnresolvableDependencies` — jinak by throw uvnitř webhook transakce vrátil i idempotenční claim a Stripe by opakoval navždy. **Vědomý trade-off:** rekonciliace běží při každém `subscription.updated` (Stripe ho posílá i za změnu karty), takže ručně vypnutý *tarifní* modul se při dalším eventu znovu zapne — per-tenant ruční vypnutí tarifního modulu není podporovaný workflow MVP. Globální superadmin kill-switch respektován (přeskočen). Revidovatelné přidáním `overridden_at` markeru
- 2026-07-23: **`verified_at` je jediná gate resolce custom domény, `ssl_status=issued` je best-effort informace (vlna 2.1).** Downstream (resolver, ask endpoint) gatuje na ověření vlastnictví, ne na existenci certu — `issued` detekuje `DomainCertProbe` a smí až 60s lagovat za realitou. `DomainTenantFinder` neresolvuje neověřenou custom doménu (grouped-OR: subdomény bez podmínky, custom jen `verified_at IS NOT NULL`), takže `forget(host)` na **každé** změně stavu (verify/promote/cert/delete) je load-bearing — bez něj by neověřená/smazaná doména žila v cache do TTL. Enum `SslStatus` beze změny (žádný nový case)
- 2026-07-23: **Ověření vlastnictví = TXT challenge token AND routing, obojí povinné (vlna 2.1).** `DomainVerifier` (jediná autorita nad `verified_at`) ověří TXT `challenge_token` na `_droidshop-challenge.<doména>` **a** routing (CNAME dot-anchored `.edge_host` NEBO A obsahuje `server_ip`). CNAME check je kotvený na tečku (`=== edge_host || endsWith('.'.edge_host)`), jinak by `notedge.droidshop.cz` prošlo. DNS za kontraktem `DnsChecker` (produkce `SystemDnsChecker`/`dns_get_record`, testy `FakeDnsChecker`) — deterministické testy
- 2026-07-23: **Caddy on-demand TLS autorizuje ask endpoint `/internal/tls-check`, ne app sám.** 200 jen pro `verified_at != null` + `type=Custom` + tenant `allowsStorefront()`; ownership povinný před `ssl=pending`, který teprve odemkne emisi (ochrana proti LE rate-limit abuse / mis-issuance). **Autentizace endpointu = shared-secret token** (`config('platform.tls_check_token')`, `hash_equals`, fail-closed na prázdný config), NE jen IP guard: za lokální reverse proxy je `REMOTE_ADDR` vždy `127.0.0.1`, takže `AllowLocalOnly` sám by z endpointu udělal enumeration oracle. Token zapečen v Caddyfile ask URL; Caddyfile navíc zamítá veřejný `/internal/*` (deploy runbook). `AllowLocalOnly` ponechán jako obrana do hloubky
- 2026-07-23: **Cert-issued flip a canonical swap jsou jedna atomická transakce (vlna 2.1).** `DomainCertProbe` obalí `ssl_status=Issued` save + `CanonicalDomain::promote` do jedné `DB::transaction` — top-of-probe guard (`Issued → return`) by jinak při selhání promote nechal doménu Issued-ale-ne-primary navždy bez retry cesty. `promote` je idempotentní no-op při už-primární. 301 subdoména→custom přes `RedirectToCanonicalHost` (storefront GET/HEAD, admin/`soubory`/`onboarding`/`impersonace` vyloučeny, custom neredirectuje sám na sebe, Location vždy z DB + `https`)
- 2026-07-25: **Branding nájemce = CSS custom properties v inline `<style>` z `tenant_theme`, ne per-tenant CSS build (vlna 2.2).** Jedno sdílené storefront CSS pro všechny nájemce (jeden build, dlouhá cache); barvy jsou jen pár proměnných v `<head>` z composeru. Per-tenant build by znamenal N buildů a invalidaci cache při každé změně barvy. `tenant_theme` je samostatná tabulka (ne sloupce na `tenants` = identita, ne `SettingsService` = modulový setting; vzhled je jádrová prezentace). Branding je per-tenant, ne per-návštěvník → cache-safe (smí do cachovaného HTML)
- 2026-07-25: **CSS-injekci hlídá `ThemeResolver::sanitizeHex`, ne jen admin FormRequest (vlna 2.2).** Barva jde do inline `<style>` a Blade `{{ }}` neutralizuje jen HTML, ne CSS znaky (`;{}()@`). Resolver odmítne cokoli mimo `/^#[0-9a-fA-F]{6}$/D` → default, takže do `<style>` nikdy nevstoupí ne-hex bez ohledu na zdroj (DB/admin/budoucí write path). `/D` modifikátor blokuje trailing-newline bypass PCRE `$`. Defense-in-depth: stejný `/D` regex i na admin vstupu
- 2026-07-25: **Favicon jen `png,ico`, ne SVG (vlna 2.2).** SVG na `tenant_public` se servíruje jako `image/svg+xml` a spustí embedded `<script>` na přímé URL = stored XSS dosažitelný nájemcem. Stejná politika jako raster-only product images (`ProductImageService::ALLOWED_MIMES` bez SVG). Logo jede na Laravel `image` validatoru, který SVG defaultně vylučuje
- 2026-07-26: **Bloky homepage = relační tabulka `homepage_blocks`, ne JSON dokument (vlna 2.3).** Řádek per blok (`position`, `type`, `payload` JSON, `visible`) dá per-blok validaci FormRequestem, řazení `position` + tlačítka nahoru/dolů (WCAG 2.1.1, precedent kategorie/obrázky) a most k budoucí blokové stavbě obecných stránek. Tabulka v modulu `storefront` (prezentace šablony, závisí na katalogu); storefront je core modul, takže modulová admin routa `admin.storefront.homepage.*` přes `module:storefront` gate vždy projde
- 2026-07-26: **Seed výchozí homepage = jedna pravda `DefaultHomepage`, core resolvuje modul stringem (vlna 2.3).** `DefaultHomepage::seed` (idempotentní, no-op při ≥1 bloku) volá `TenantProvisioner` (noví) i backfill migrace (stávající) — žádná druhá cesta (precedent `DemoShopSeeder`→provisioner). Provisioner resolvuje seeder přes `app('Modules\\Storefront\\Support\\DefaultHomepage')` **stringem, ne `::class`** — pint `fully_qualified_strict_types` by `::class` povýšil na `use Modules\…` a vnesl jediný compile-time modulový import do `app/Core` (konvence: core zná modul jen stringem, viz `ModuleRegistry`)
- 2026-07-26: **`BlockUrl::isSafe` odmítá i protocol-relative a backslash, nejen `javascript:` (vlna 2.3).** URL v payloadu (hero `cta_url`, banner `url`) jde do `href`. Interní cesta `/…` je povolená, ale `//evil.com` (browser → `https://evil.com`) a `/\evil.com` (WHATWG backslash normalizace) taky začínají `/` — open-redirect/phishing maskovaný jako interní odkaz. Guard proto: `/` ANO, ale `//`/`\` NE; `^https?://` ANO; zbytek NE. Blade escape brání XSS, guard brání open redirectu
- 2026-07-26: **`image_path` bloku je server-authoritative, editace přes POST+`_method` spoofing (vlna 2.3).** `HomepageAdminController::update` po `cleanPayload` **odstraní** klientem poslaný `image_path` a nastaví ho jen z reálného uploadu (`FileStorage::putPublic`) nebo z předchozí uložené hodnoty — klient ho nikdy nediktuje. Editor uploaduje přes `router.post({..., _method:'patch'}, {forceFormData:true})`: PHP neparsuje multipart tělo na nativním PATCH, takže literal `router.patch` s file by tiše zahodil celý payload. Text `html` sanitizuje `cleanPayload` (`HtmlSanitizer`) **při zápisu**, nikdy se neukládá raw
- 2026-07-26: **Jeden `hero` blok na homepage (jeden `<h1>`), page cache invalidace odložená (vlna 2.3).** `store` odmítne druhý `hero` a editor ho odfiltruje z nabídky — víc `<h1>` by rozbilo nadpisovou navigaci (WCAG 1.3.1). Page cache invalidace po editaci bloků **není** implementovaná, protože page cache samotná zatím není (jen budoucí klíč v `bootstrap/app.php`); až přijde, doplní se hook
- 2026-07-23: **`domains:sweep-pending` (hodinově): DNS chyby auto-retry, cert chyby terminální (vlna 2.1).** Group A (neověřené) → verify; verify-error se retruje automaticky (DNS propaguje), ale po `pending_ttl_hours` → error **jednou** (exclude už-timedout řady, jinak nekonečný audit growth). Group B (verified + `ssl=pending`) → cert probe; cert-error po vyčerpání pokusů je terminální, re-trigger jen ruční přes admin „Ověřit teď" (`DomainController::verify` reset+probe). Limit 1 custom doména/tenant app-level (FormRequest), bez DB constraintu (cross-DB partial index křehký) — souběžný double-submit je low-impact self-inflicted risk
- 2026-07-26: **JS ostrůvek variant je vanilla JS v `resources/js/storefront.js`, ne Alpine, jak psal design spec (vlna 2.4).** Projekt Alpine nemá (`package.json` = Vue + Inertia + Tailwind) a závislosti se nemění bez souhlasu uživatele. Ostrůvek se napsal stejným stylem jako existující galerie ve stejném souboru — čte embedded `<script type="application/json" data-variant-matrix>`, na `change` najde přesnou shodu a jen přepíše text/atributy, žádná cenová aritmetika v JS. Přidání Alpine kvůli jednomu ostrůvku by znamenalo novou runtime závislost na storefrontu za cenu, kterou vanilla JS zvládne stejně dobře
- 2026-07-26: **`cart_items.variant_id` je NOT NULL se sentinelem `0`, `order_items.variant_id` nullable bez FK — vědomě asymetrické (vlna 2.4).** MySQL i SQLite berou `NULL` v unique indexu jako vždy odlišný, takže nullable `cart_items.variant_id` by nechal produkt bez variant vložit do košíku vícekrát jako samostatné řádky — přesně to, čemu `cart_item_unique` má zabránit. `order_items` unique index nemá, takže tam `NULL` čte poctivě jako „bez varianty" a sentinel by byl matoucí navíc; FK tam chybí záměrně stejně jako u `product_id` — objednávka je snímek, který musí přežít smazání varianty
- 2026-07-26: **`variant_display` sedí na `tenant_theme`, ne v `settings_schema` modulu `products` (vlna 2.4).** `SettingsService` dnes nemá žádnou admin obrazovku — postavit **první** obrazovku modulových nastavení kvůli jednomu přepínači by byl scope navíc mimo cíl vlny. Pole je proto vedle loga/barev na `/admin/nastaveni/vzhled`, přestože jde o katalogovou prezentaci, ne branding. Až vznikne obrazovka modulových nastavení, pole se přesune
- 2026-07-26: **Cross-tenant admin přístup na variantách vrací 403, ne 404, jak předpokládal implementační plán (vlna 2.4).** Signed-in uživatel, který není členem tenanta, dostane `403` — existující konvence `EnsureTenantMember` (routa už tím, že prošla modulovou branou, prozrazuje svou existenci; precedent `DocumentAdminTest`). Přepsání middlewaru na 404 kvůli jednomu novému testu by rozbilo tuhle záměrnou hranici jinde v adminu. Cizí **produkt** id (člen tenanta A, produkt tenanta B) 404uje beze změny — tenant-scoped route-model binding ho prostě nenajde
- 2026-07-26: **JS ostrůvek variant aktualizuje i cenu „bez DPH", embedded matice proto nese server-formátovaný `net_price` (vlna 2.4).** Bez toho by po přepnutí varianty JS aktualizoval hrubou cenu, ale nechal viset zastaralou čistou cenu vedle ní — nekonzistentní pár čísel na obrazovce. `net_price` počítá `variant-picker.blade.php` stejnou cestou jako `show.blade.php` (`$product->rate()->net(...)`, `TaxRate`, ne `Money`) — konverze DPH zůstává na serveru, JS jen zobrazí druhý předpočítaný řetězec

- 2026-07-27: **Šifrování `shipping_methods.settings` — revize rozhodnutí z 2026-07-21 (vlna 2.5).** „Dopravní nastavení nejsou tajná" platilo jen dokud sloupec nesl výdejní adresu a otevírací dobu. `apiPassword` Zásilkovny je credential podle §16.5, takže sloupec se přesouvá k `payment_methods.settings`: `encrypted:array`, v adminu jen boolean „heslo je uloženo", keep-on-update. Migrace jednorázově re-encryptuje existující řádky. **Háček nalezený review:** `$dontFlash` na FormRequestu je v Laravelu **mrtvý kód** (nikdy se nečte) — mechanismus žije jen na exception handleru, takže vyloučení credentialu z flashovaného inputu patří do `bootstrap/app.php` a `dontFlash()` je tam aditivní k výchozím password polím
- 2026-07-27: **Výběr výdejního místa je server-rendered a je to primární cesta, ne fallback (vlna 2.5).** Katalog míst se denně syncuje do naší netenantové tabulky, takže hledání běží nad vlastní databází a **výpadek Zásilkovny nezastaví nákup** — jen odloží podání. Oficiální widget je vanilla ostrůvek (žádné Alpine, precedent 2.4) načítaný **až na kliknutí**: dokud zákazník mapu nechce, checkout nedělá žádný request na cizí doménu (výkon, ePrivacy, CSP). Widget i bez-JS formulář posílají **stejné jedno pole** na stejný endpoint, takže na serveru existuje jedna cesta. Klient posílá jen kód; název a adresa se vždy čtou z katalogu, nikdy z requestu — stejná politika jako `variant_id` (2.4) a `image_path` (2.3)
- 2026-07-27: **Sync katalogu odmítne prázdný nebo useknutý feed (vlna 2.5).** `whereNotIn('code', [])` se v Laravelu překlopí na `1 = 1`, takže prázdná množina „viděných" kódů znamená „deaktivuj všechno". Guard proto měří **počet použitelných kódů po zpracování**, ne délku odpovědi — feed s dostatkem položek, ale bez `id`, projde prvním měřením a smaže katalog všem nájemcům. Práh v `packeta.feed_min_points`
- 2026-07-27: **Podání zásilky: claim se commitne, teprve pak jde HTTP (vlna 2.5).** Cizí volání nikdy uvnitř transakce — tech dluh 1.8 byl přesně tohle. Cena té volby je `pending` řádek po pádu procesu, takže retry ho musí umět převzít. Ochrana proti dvojímu podání **nesmí stát na unique indexu** (ten chrání řádek, ne HTTP volání): dva souběžné požadavky adoptovaly tentýž `pending` řádek a oba zavolaly dopravce. Řeší to compare-and-swap claim (`UPDATE … WHERE status IN (…)`, volá jen vítěz), fencing finálního zápisu (opožděný request nepřepíše výsledek toho, kdo řádek převzal), staleness reclaim (zaseknutý `submitting` se po prahu uvolní) a fail-fast guard, že práh je aspoň 2× HTTP timeout — jinak by reclaim sáhl na ještě běžící volání a vyrobil dva reálné balíky
- 2026-07-27: **Modul `packeta` deklaruje jen `packeta.ship`; `packeta.manage` ze specu neexistuje (vlna 2.5).** Credentials Zásilkovny sedí na obrazovce dopravy za `shipping.manage`, takže druhé právo nikde nic nehlídalo — udělení `packeta.manage` nedávalo uživateli nic. Klamná autorizační plocha je horší než chybějící: až přijde `TENANT_STAFF`, někdo by jí uvěřil. Věcně AK platí dál (API heslo se do adminu nikdy nevrací)
- 2026-07-28: **Sleva se alokuje per řádek, ne jako jedna částka (vlna 2.6).** DPH rekapitulace se počítá po sazbách ze součtů řádků, takže jedna lump-sum sleva by musela sednout na jedinou sazbu — špatně pro košík s mixem 21 %/12 % zboží. `DiscountAllocator` rozpouští přes `Money::allocateByRatios()` a je capacity-aware: víc slev cílících na tentýž řádek nikdy dohromady nepřekročí jeho vlastní součet (sloupce jsou `unsignedInteger` — přeplacení by selhalo hlasitě, ne se tiše otočilo do záporu)
- 2026-07-28: **Klíč modulu je `discounts`, ne `coupons` ze spec §909 (vlna 2.6).** Modul obsluhuje i automatická pravidla bez kódu (např. „doprava zdarma nad X"); `coupons` by obsah podhodnocoval a byl by matoucí, až přibudou akční ceny produktu (vlna 2.7), které s kódem nemají nic společného
- 2026-07-28: **Vlastní `combinable` příznak kupónu se ignoruje — čte se jen u automatických pravidel (vlna 2.6).** Kupón je vždy nejvýš jeden na košík, takže „smí se kombinovat s čím" u něj nedává smysl; formulář v adminu proto `combinable` u kupónu vůbec nenabízí, aby nevznikl ovladač bez efektu
- 2026-07-28: **Čerpání limitu se spotřebuje při odeslání objednávky uvnitř téže transakce jako odpis skladu, vrací se všude, kde se vrací sklad — gated na `returnStock` (vlna 2.6).** Podezřelý storno, který sklad záměrně nevrací, tak nevrací ani kupón. Pořadí zámků je produkty nejdřív, řádek slevy poslední; vrácení proto musí běžet až po vrácení skladu, jinak by šlo o ABBA deadlock proti souběžnému odesílání objednávek
- 2026-07-28: **Odmítnutý kupón nikdy neuloží objednávku tiše za plnou cenu — a to pro libovolný důvod odmítnutí, ne jen pro e-mailem gatovaný (vlna 2.6, rozšířeno final review).** Původní pravidlo se týkalo jen `usage_limit_per_email`/`first_order_only` (košík e-mail nezná, takže jen ty dva se mohou při odeslání dozvědět jinou odpověď). Ostatní důvody (`expired`, `inactive`, `usage_limit`, `min_cart`, `no_eligible_items`, `requires_login`) přestaly slevu jen tiše aplikovat a objednávka se zapsala za plnou cenu — zákazník tak zaplatil víc, než ukazovalo tlačítko platební povinnosti, když mu někdo sebral poslední použití kupónu nebo `ends_at` přešlo přes půlnoc. Zúžení mělo reálný důvod: kód, který rekapitulace **už** zobrazila jako odmítnutý, se nesměl stát slepou uličkou při odeslání, a nic nenese informaci o tom, co rekapitulace zobrazila. Řeší to `Modules\Checkout\Support\StaleCoupon`: každý render košíku i rekapitulace pokladny odmítnutý kód **smaže z košíku** a na téže stránce vypíše důvod, takže „kód je stále na košíku" znamená „při posledním renderu platil". Až pak smí `OrderPlacer::refuseIfTheCouponStoppedApplying()` odmítnout na cokoli. Mazání sedí v controllerech (`CartController::show`, `CheckoutController::details`), **ne v `CartPricer`** — ten musí zůstat čistý read, protože mini-košík (`GET /api/kosik/souhrn`) ho volá na každém zobrazení storefrontu a zapisující pricer by měnil obsah košíku z background fetche bez stránky, kde to zákazníkovi vysvětlit
- 2026-07-28: **Editace objednávky v adminu zachová podíl slevy na každém přeživším řádku a znovu odvodí `orders.discount_total`; engine se znovu nespouští (vlna 2.6).** Snímkové řádky `order_discounts` se editací nedotýkají, takže poznámka na faktuře čte živé číslo a jména slev vynechá, jakmile se rozejde se snímkem
- 2026-07-28: **Objednávka za 0 Kč po slevě se settluje přímo přes `OrderSettlement`, nikdy nejde na bránu — Comgate nulovou částku odmítá (vlna 2.6).** QR platba i platební instrukce se proto potlačí na děkovné stránce i v potvrzovacím e-mailu
- 2026-07-28: **Vrácení čerpání kupónu je compare-and-swap a `OrderEditor::cancel()` bere zámek na řádku objednávky (vlna 2.6, final review).** `SELECT … WHERE released_at IS NULL` + bezpodmínečný `UPDATE` nechrání nic: dva souběžné releasy téže objednávky vidí řádek jako nevrácený oba a oba dekrementují `used_count` — kupón s `usage_limit = 100` jde použít 101×. Stamp proto znovu tvrdí `released_at IS NULL` uvnitř `UPDATE` a dekrementuje jen ten, komu UPDATE zabral řádek. `cancel()` navíc nebral žádný zámek, kdežto kontrola přechodu v `OrderWorkflow` je čistě paměťový lookup nad statusem předané instance — dvojklik na „Stornovat" tak protlačil celé tělo dvakrát (dvojí vrácení skladu i kupónu). Zamyká se stejně jako `EloquentOrderSettlement::settleFailed()` a pracuje se zamčenou instancí; pořadí zámků beze změny (produkty nejdřív, řádek slevy poslední)
- 2026-07-28: **Kupónový endpoint throttluje ve FormRequestu, protože slevový kód je hádatelné tajemství (vlna 2.6, final review).** `POST /kosik/sleva` neměl žádný limit (skupina `web` ho nenese) a jeho odpovědi rozlišují „takový kód neexistuje" od `min_cart`/`expired`/`usage_limit`/`requires_login` — to je slovníkový oracle nad kompletním seznamem kupónů nájemce. `ApplyDiscountRequest` throttluje v `passedValidation()` (10 pokusů/min na tenant + cart token + IP, `ValidationException` s českou zprávou), takže odmítnutý pokus se k vyhledání slevy vůbec nedostane. Stejný `RateLimiter` vzor jako `Modules\Customers\Http\Requests\LoginRequest` — žádný nový mechanismus. Počítá se každý pokus včetně úspěšného: „úspěch je zdarma" by útočníkovi dalo neomezené sondy na kódy, které uhodl správně
- 2026-07-28: **Akční cena je katalogová cena, ne modifikátor (vlna 2.7).** `ProductCatalog::price()` a `catalogPrice()` vracejí po dobu kampaně `sale_price`, takže košík, `OrderPlacer`, doklady i slevový engine 2.6 účtují správně bez jediné změny volajícího kódu — a kupón se automaticky počítá z akční ceny, ne z nominální. Nominální cena je dostupná zvlášť (`catalogRegularPrice()`) jen pro přeškrtnutí. Sazba DPH se nemění, mění se základ; `netPrice()`/`vat()` proto počítají z efektivní ceny
- 2026-07-28: **Okno kampaně sedí jen na produktu, částka i na variantě (vlna 2.7).** Dvě nezávislá okna by dovolila stav „varianta je v akci, produkt ne". Varianta bez vlastní `price` dědí i produktovou `sale_price`; varianta s vlastní `price` musí mít vlastní `sale_price`, jinak jede na nominální — absolutní částka zděděná na jiný cenový základ by tiše prodávala pod nákladem
- 2026-07-28: **Historie ceny je časová řada s dopředu zapsanými plánovanými intervaly (vlna 2.7).** `PriceHistoryRecorder` při uložení kampaně zapíše i interval `[start, konec)` a návratový řádek — konec akce tak nepotřebuje cron ani job, protože už leží v tabulce. Invariant: **uzavřený** řádek se nikdy nemění, běžícímu se smí posunout jen konec, který ještě nenastal (bez toho by přeplánování kampaně vyrobilo dva intervaly překrývající se v čase). Denní snapshot zamítnut: roste s katalogem a mine akci kratší než den
- 2026-07-28: **Referenční okno „nejnižší cena za 30 dní" končí startem běžící kampaně, ne přítomností (vlna 2.7, oprava proti vlastní specifikaci).** Zákon žádá cenu *před poskytnutím slevy*; kdyby se akce počítala do svého vlastního okna, reference by se vždy rovnala akční ceně a každá oznámená sleva by vyšla 0 %. Procento se počítá z této reference, ne z nominální ceny — to je číslo, které čte ČOI. Produkt nasazený rovnou do akce nemá starší historii, takže reference spadne na aktuální cenu: řádek se zobrazí, badge ne (oznámení slevy bez reference je horší než bez procenta)
- 2026-07-28: **`tax_rate_id` u dopravy a platby je povinné pro plátce DPH (vlna 2.7, uzavření dluhu 2.6).** Poplatek bez sazby se účtoval, ale vypadával z DPH rekapitulace, takže součet dokladu neseděl s vlastním daňovým rozpisem. Neplátce ho dál nemusí vyplňovat (jeho rekapitulace je prázdná); existující metody plátců dostaly `TaxRates::default()` backfill migrací. Tichý fallback na výchozí sazbu zamítnut — účetní číslo nemá vznikat z domněnky, kterou nájemce nikdy nepotvrdil
- 2026-07-28: **Formát CSV drží jediná třída pro import i export (vlna 2.8).** `ProductCsvSchema` nese sloupce, stavy, skladové politiky i převod peněz; export produkuje přesně to, co import přijme, a `CsvRoundTripTest` to hlídá. Dvě nezávislé definice formátu by se rozešly při prvním přidaném sloupci a nájemce by to zjistil až tím, že mu import zahodí data
- 2026-07-28: **Import zapisuje výhradně přes `ProductWriter`/`VariantWriter` (vlna 2.8).** Přímý `Product::create()` by obešel sanitizaci HTML, unikátní slug, 301 redirect při změně slugu i zápis do `product_price_history` — a tím by hromadné nahrání cen tiše rozbilo zákonnou evidenci nejnižší ceny z vlny 2.7
- 2026-07-28: **Klíč importu je SKU; kategorie se nezakládají, výrobce ano (vlna 2.8).** SKU je to, co nájemce má ve skladovém systému i u dodavatele; prázdné zakládá nový produkt, duplicitní (v souboru i v DB) je chyba řádku, protože import nemá jak vybrat. Kategorie je **sdílený strom**, kde překlep v jednom z 3 000 řádků vyrobí větev k ručnímu úklidu, takže neexistující cesta řádek odmítne; výrobce je plochý štítek, u kterého je stejná chyba levná, a `ProductWriter::manufacturer()` ho firstOrCreate zakládal už dřív
- 2026-07-28: **Chybný řádek nezastaví běh a prázdná buňka při aktualizaci nemaže (vlna 2.8).** Jedna transakce na řádek (ne kolem celého běhu, který by držel zámky nad celým katalogem), chybné řádky do protokolu s číslem řádku a důvodem, plus přepínač „jen zkontrolovat", který jede stejnou cestou bez zápisu. Prázdná buňka znamená „neměnit" — opačná volba by z prázdného sloupce v Excelu udělala smazané popisy celého katalogu
- 2026-07-28: **Import a export routy leží nad `/{product}` (vlna 2.8).** Produkt se váže slugem, takže `/export` a `/import` registrované za ním by Laravel hledal jako produkty toho jména. Právo je `products.edit`, žádné nové `products.import` — právo, které nic navíc nehlídá, je klamná autorizační plocha (stejný závěr jako u `packeta.manage` ve vlně 2.5). Nákupní cena je v exportu jen pro `products.costs`, aby export nebyl zadní vrátka k marži
- 2026-07-28: **Modul `feeds` je `base`, ne premium, a musí být přiřazený všem tarifům (vlna 2.9).** Feed do Heureky je v Česku podmínka prodeje; schovat ho za vyšší tarif by srazilo hodnotu základního tarifu pod běžný standard. Modul, který není v žádném tarifu, přitom fakticky neexistuje — nájemce dostane `PlanDoesNotIncludeModule` — takže vedle manifestu je potřeba i migrace přiřazující modul tarifům, řádek v `PlanSeeder` a test, který to hlídá (odhaleno až ručním ověřením, ne testy)
- 2026-07-28: **Vypnutý feed vrací 404, ne prázdné XML (vlna 2.9).** Prázdný feed čte porovnávač jako „e-shop nemá zboží" a odstraní z výpisu celý katalog; 404 je stav, na který má připravenou reakci. Totéž platí pro vypnutý modul, což řeší `module:feeds` gate v `ModuleRouteRegistrar` bez zvláštního kódu
- 2026-07-28: **Mapování kategorií porovnávače je vlastní tabulka `feed_category_mappings`, ne sloupce na `categories` (vlna 2.9).** Modul `feeds` jde vypnout a nesmí po sobě nechat sloupce v cizím modulu. Prázdné mapování degraduje na vlastní strom nájemce — feed bez mapování je horší, ale funkční, kdežto odmítnutí publikovat by nájemce stálo víc než nepřesná kategorie. Resolver chodí po `parent_id`, ne po materializované `path`, protože tu plní až `CategoryWriter`
- 2026-07-28: **Varianta je ve feedu samostatná položka s `ITEM_ID` ve tvaru `{produkt}-{varianta}` (vlna 2.9).** Oba porovnávače to tak čekají a zákazník pak najde konkrétní velikost. Holé id varianty by mohlo kolidovat s id produktu a porovnávač by položky spároval přes sebe. Vyprodané zboží zůstává ve feedu s `DELIVERY_DATE`, aby položka neztrácela historii a hodnocení

- 2026-07-30: **Právo k nastavení modulu jmenuje manifest (`settings_permission`), konvence `<klíč>.manage` nestačí (vlna 2.10).** Reálná práva se rozcházejí: `docs.manage`, ale `products.edit` a `orders.edit`. Odvozovat právo z klíče modulu by u poloviny modulů mířilo na právo, které neexistuje, a `Gate::allows()` na neexistující právo vrací false pro všechny — obrazovka nastavení by byla nedosažitelná i pro vlastníka. `ManifestValidator` křížově kontroluje, že jmenované právo modul opravdu deklaruje, takže překlep padne při `modules:sync`. Modul **bez** `settings_permission` nesmí nastavovat nikdo: fallback na „kdokoli v adminu" by dal personálu číselnou řadu modulu, na který jinak nemá právo
- 2026-07-30: **Obrazovka nastavení modulu sedí v jádře (`/admin/nastaveni/moduly/{modul}`), ne pod `/admin/m/{modul}/nastaveni` (vlna 2.10).** Modulová cesta by u `products` kolidovala s vazbou produktu na slug (`/admin/m/products/{product}`) — `nastaveni` by Laravel hledal jako produkt toho jména. Stejný důvod, proč import/export routy leží nad `/{product}` (vlna 2.8). Formulář je generovaný ze schématu, takže nový modul dostane nastavení bez vlastní obrazovky; jádro zná modul jen klíčem a schématem, ne třídou
- 2026-07-30: **Vadné schéma nastavení shodí `modules:sync`, ne až request (vlna 2.10).** Pole bez `rules` by tiše přijalo cokoli, takže `SettingsSchema::fromArray()` ho odmítá. Kontrola ale patří k deploji: `SettingsService::all()` běží na hot path (checkout, vystavení faktury, generování PDF), a hlasitý pád tam by odnesl zákazník uprostřed objednávky. Runtime cesta výjimku přesto propaguje dál — druhá brána nesmí tu samou vadu schovat za tiše prázdné pole
- 2026-07-30: **Zápis nastavení je all-or-nothing (`SettingsService::setMany()`, vlna 2.10).** Validace celé sady, teprve pak zápis v jedné transakci. Po jednom by šestá odmítnutá hodnota nechala e-shop běžet na míchanici starého a nového nastavení a nic na obrazovce by to neřeklo. `all()` navíc slévá defaulty ze schématu, takže schéma je jediná pravda o tom, na čem běží nedotčený e-shop — ne `get('docs','due_days', config(...))` rozeseté po volajících
- 2026-07-30: **Minimum objednávky se měří na zboží po slevě, bez dopravy a platby; obě checkout pravidla vynucuje server v `details()` i `place()` (vlna 2.10).** Kdyby se do minima počítala doprava, drahý dopravce by zákazníka přenesl přes hranici, kterou si nájemce sám nastavil. Skryté tlačítko „Pokračovat k pokladně" je prezentace, ne pravidlo — zapomenutá karta v prohlížeči se přes minimum neprotlačí. `guest_checkout = false` posílá hosta na přihlášení přes `redirect()->guest()`, takže po přihlášení pokračuje tam, kde byl
- 2026-07-30: **`variant_display` se přesunul z `tenant_theme` do nastavení modulu `products` a sloupec se dropnul (vlna 2.10, uzavření odchylky 2.4).** Migrace hodnoty přenese **před** dropem, jinak by se e-shop nastavený na rozbalovací seznam po deployi tiše přepnul na přepínače. `VariantDisplay::sanitize()` zůstává poslední pojistkou: hodnota mimo dvojici `radio`/`select` míří do Blade branche, která by nevykreslila žádný widget a produkt by nešel koupit
- 2026-07-30: **Prefix čísla objednávky je nastavení nájemce, ne součást řádku v `sequences` (vlna 2.10).** Změna prefixu tak nikdy nepřepíše čísla, která už byla vydána — stejné dělení, jaké `InvoiceIssuer` používá pro čísla dokladů
- 2026-07-30: **Složení tarifu se rekonciluje proti živě zapnuté sadě, ne proti diffu editace (vlna 2.10).** `PlanModuleReconciler::apply()` je proto idempotentní a nezávislý na pořadí (stejný tvar jako `TenantPlanSwitcher` z 1.9). Katalog „co vůbec smí tarif udělovat" se snímá **před** zápisem: přečtený po `sync()` už neobsahuje klíč, který tahle editace odebrala, a nevypnulo by se nikdy nic. Aktivace se protíná s `ModuleRegistry::available()`, takže globálně kill-switchnutý modul se přeskočí místo výjimky, která by shodila změnu dotýkající se mnoha e-shopů kvůli jednomu modulu, který už incident vyřadil. Core moduly nejsou v `plan_modules` — to je jediné, co je drží mimo dosah deaktivace. Povinný důvod se vyžaduje podle **spočítaného dopadu**, ne podle formuláře: jestli se něco reálně ztrácí, závisí na tom, co ty e-shopy dnes běží

- 2026-07-30: **Core moduly nepatří do `plan_modules` a oba rekonciliační callery si to samy vynucují (oprava po vlně 2.10).** Core modul běží v každém e-shopu bez ohledu na tarif (`enabledFor()` ho vždy vrací, `guardPlan()` ho vynechává), takže grant řádek neuděluje nic. Nebyl to ale jen šum: `PlanModuleReconciler` i `TenantPlanSwitcher` odvozují „co smí tarif odebrat" z `plan_modules`, takže core klíč v tabulce spadl do deaktivační sady, kde `ModuleRegistry::deactivate()` vyhodí výjimku — u superadmin obrazovky po už provedeném `sync()` (napůl aplikovaná změna), u Stripe webhooku uvnitř jeho transakce, což vrátí i idempotenční claim a Stripe doručuje event navždy. Řádky tam dostal `DemoShopSeeder` (přiřazoval demo tarifu **všechny** nasazené moduly). Oprava: oba callery core klíče odečítají **explicitně**, ne na základě předpokladu zapsaného v komentáři; migrace core řádky z `plan_modules` uklidila a seeder je přeskakuje. Počítadlo modulů v seznamu tarifů počítá jen grantable moduly, aby seznam a detail hlásily totéž

- 2026-07-30: **Tarifní příslušnost modulu se odvozuje z `level` v manifestu (`PlanModuleDefaults`), ne z migrace per modul.** `level` byl dosud jen nálepka — `guardPlan()` se ptá výhradně na řádek v `plan_modules` — a ordinární moduly tam nikdo nikdy nevložil: v čerstvé produkční DB udělovaly tarify jen `discounts` (premium) a `feeds` (všechny), takže onboardovaný e-shop naběhl **bez katalogu a bez pokladny**. Pravidlo je teď na jednom místě: `core` → žádný tarif (běží všude, grant řádek by nic neudělal a spadl by do deaktivační sady), `level: base` → všechny tarify, `level: premium` → jen premium. Volá ho `PlanSeeder`, backfill migrace a `modules:sync` **jen pro modul, který právě vytvořil** — vědomé odebrání modulu z tarifu na `/superadmin/tarify` tak deploy nepřepíše. Nový modul proto nepotřebuje vlastní attach migraci, jen správný `level`. **Rozdělení (rozhodnutí vlastníka): base = celý prodejní e-shop** (katalog, pokladna, objednávky, doprava, platby, zákazníci, stránky, faktury, feedy, Zásilkovna), **premium = base + marketingové nástroje** (dnes `discounts`) a vyšší limity (produkty 500→5000, storage 2→20 GB, e-maily 3k→30k). `DemoShopSeeder` proto zakládá demo na **premium** a nepřiřazuje moduly tarifu sám — dřív demo tarifu přiřazoval všechno včetně core, a právě tím do `plan_modules` dostal `storefront`

- 2026-07-30: **Modul `accounting` (premium) exportuje doklady do Pohoda XML a ISDOC; registry formátů žije uvnitř modulu, ne v jádře (vlna 2.11).** Kontrakt `AccountingFormat` + `AccountingFormats` podle klíče (vzor `PaymentGatewayRegistry`); jádrový kontrakt by měl jediného implementátora a nešel by vypnout. Modul **nedeklaruje `requires` na `docs`** (precedent `checkout`/`orders`) — bez něj vrací null binding `DocumentLedger` prázdno a obrazovka řekne „žádné doklady“. `DocumentLedger` dostal `findTaxDocument($number, $type)`, kde **typ je povinný argument**: číslo je od vlny 1.6 unikátní jen v rámci `(tenant, type)`, takže by stará URL vydala fakturu na číslo dobropisu. **Odchylka schválená vlastníkem:** formátové writery čtou model `Document` přímo, ne přes kontrakt — porušuje rozhodnutí z 2026-07-22, cena je tiché degradování na prázdná pole při změně snímku
- 2026-07-30: **Ceny ve snímku dokladu jsou s DPH, účetní formáty chtějí bez DPH — převod dělá `DocumentLines` přes `TaxRate::net()` celočíselně (vlna 2.11).** První verze psala hrubé částky do polí, která jsou v Pohodě i ISDOC definovaná jako bez daně, takže by import seděl zhruba o 21 % výš; golden files to nechytily, protože porovnávaly jen strukturu (proto k nim přibyly hodnotové aserce počítané nezávisle na exportéru). Pohoda navíc značí `inv:payVAT`. **Snímek přitom nenese dopravu ani poplatek za platbu**, takže řádky neseděly s `documents.total`; modul rozdíl dopočítá per sazba jako řádek „Doprava a poplatky“. To je léčba symptomu — skutečná díra je v `InvoiceSnapshot` a **týká se i zákaznického PDF**, kde se řádky nesečtou na „Celkem k úhradě“
- 2026-07-30: **Účetní export selhává hlasitě, nikdy tiše neúplně (vlna 2.11).** Neznámá sazba DPH zastaví export výjimkou, která jmenuje doklad (422, přesné 21/12/0, žádné zaokrouhlování — 11,5 % neprojde jako `low`); `ZipArchive::addFromString()` i `close()` se kontrolují, protože archiv s tiše chybějící fakturou je horší než pád; prázdné období nevydá soubor, ale hlášku (prázdný soubor u účetní vypadá jako „nic se neprodalo“); audit `accounting.exported` se zapisuje **až po** úspěšném vygenerování. ISDOC `UUID` je deterministické (v5 nad tenant+typ+číslo), protože importní nástroje na něm dedupují a náhodné by z opakovaného exportu udělalo druhý doklad

- 2026-07-31: **Doklad nese dopravu i poplatek za platbu jako řádky; součet řádků se musí rovnat částce k úhradě (vlna 2.12).** Do té doby žily jen v součtu a v DPH rekapitulaci, takže faktura tiskla položky za 1 998 Kč pod hlavičkou „Celkem k úhradě 2 097 Kč“ a zákazník neměl z dokladu jak zjistit, za co platí. `InvoiceSnapshot` i `ProformaSnapshot` skládají řádky z `orderShippingSnapshot()`/`orderPaymentSnapshot()` ve **stejném tvaru** jako položky, takže se nemění tabulka, PDF šablona ani modul `accounting`. Účtuje se `charged`, ne `price` (liší se, když sleva udělala dopravu zdarma); sazba se dohledá přes `TaxRates::findById()` a u neplátce je **nula, ne dohadovaný default**; nulový řádek se zobrazuje, aby bylo vidět, co si zákazník zvolil. **Historické doklady se nemění** (immutable snímek, PDF už odešla) — export si u nich dopočte řádek „Doprava a poplatky“, který u nových dokladů vyjde na nulu a sám zmizí
- 2026-07-31: **ISDOC se validuje proti oficiálnímu XSD 6.0.1 v testu, ne až při nasazení (vlna 2.12).** Schéma je vendorované v `tests/Fixtures/isdoc/` (validace přes vestavěný `libxml`, žádná nová závislost). Odhalilo **osm skutečných porušení** — mimo jiné `TaxSubTotal` pod jménem elementu `ClassifiedTaxCategory`, `TaxPointDate` psaný jako prázdný řetězec místo vynechání a dvě sady povinných polí pro zúčtování záloh. Každý ISDOC vydaný předtím byl odmítnutelný, a golden files to odhalit nemohly, protože porovnávají výstup se sebou samým; cizí schéma je jediná nezávislá autorita. Reálný import do Pohody zůstává ruční (potřebuje licenci)
- 2026-07-31: **Fakturační profil nájemce sbírá zemi (ISO 3166-1 alpha-2), doklad bez ní jede na `CZ` (vlna 2.12).** Bez toho by ISDOC v produkci vyšel s prázdnou zemí dodavatele — testy to nechytaly, protože si ji fixture do adresy vkládal sám. `CZ` není dohad: platforma účtuje v CZK, vyžaduje IČO a jede na českých daňových pravidlech. Fallback sedí ve **snímku**, ne v exportním modulu, takže z něj těží i zákaznické PDF; controller předvyplní `CZ` starému profilu, aby ho povinné pole nezablokovalo. Regex má modifikátor `/D` — bez něj PCRE pustí `"CZ\n"` (stejná past jako u hex barev, rozhodnutí 2026-07-25)

- 2026-08-03: **Page cache je jádrová infrastruktura (`app/Core/PageCache/`), ne modul (vlna 3.0).** Slouží všem storefront modulům (`storefront`, `products`, `categories`, `pages`, `feeds`); modul, který jde vypnout, by ji držet nemohl
- 2026-08-03: **Invalidace generačními čítači, ne tagy (vlna 3.0).** Spec §15.6 psala tagy, ale ty umí jen Redis a projekt je dosud nikde nepoužívá. Tag by z Redisu udělal tvrdou závislost, jejíž absence by se projevila **tiše** — cache by přestala invalidovat, ne spadnout. Generační čítač funguje na file, database i Redis driveru, nevyžaduje enumeraci klíčů a nemá závod
- 2026-08-03: **Čítače leží ve sloupcích `tenants`, ne v cache store (korekce při plánování, vlna 3.0).** Čítač uložený v cache může být vystěhován; vrátí se na 1 a stránka uložená pod starou generací 1 obživne — cache by servírovala obsah, který nájemce dávno změnil. `DomainTenantFinder` beztak načítá řádek nájemce každým requestem, takže tři celočíselné sloupce nestojí ani jeden dotaz navíc a inkrement je atomický `UPDATE`
- 2026-08-03: **Invalidaci spouští observery modelů, ne volání ve writerech (korekce při plánování, vlna 3.0).** `ProductWriter` a `VariantWriter` mají dohromady přes patnáct zapisujících metod a přibývají; instrumentovat každou znamená, že šestnáctá se zapomene. Observer chytí i cesty, které teprve vzniknou (CSV import). Výjimka je odpis skladu a reorder kategorií: obojí píše přes query builder, takže nevyvolá Eloquent event a hranici musí zvednout sám volající
- 2026-08-03: **Cache se zapíná opt-in na routě** (`page-cache:{dimenze}` middleware parametr), ne globálně — nová routa se necachuje, dokud to někdo vědomě nenapíše
- 2026-08-03: **Přihlášený zákazník cache obchází celá, ne přes ostrůvek ani zástupný znak (vlna 3.0).** Hlavička renderuje „Můj účet" vs „Přihlásit se" přímo v HTML (per-návštěvníkový obsah). Zástupný znak i pro účet by přenášel povinnost pamatovat na značku na každý budoucí osobní prvek v šabloně; ostrůvek by bez JS odkaz na přihlášení úplně skryl. Přihlášených zákazníků je menšina, drtivá většina prokliků z Heureky a Googlu je anonymní
- 2026-08-03: **CSRF token se v cache nahrazuje HTML-komentářovou značkou, ne libovolným řetězcem (oprava po review, vlna 3.0).** Substituce běží nad vyrenderovanou hodnotou, ne nad Blade direktivou, takže formulář přidaný později je pokrytý bez zásahu do této třídy. Review Tasku 4 ale našlo, že prostá značka (`@@PAGECACHE_CSRF@@`) projde Blade escapingem a `HtmlSanitizer` beze změny — nájemce, který si ji napíše do popisu produktu, by dostal živý CSRF token náhodného návštěvníka vlepený do svého obsahu. Tvar `<!--PAGECACHE_CSRF-->` tohle vylučuje: stejná značka napsaná do textového pole vyjde z Blade jako `&lt;!--PAGECACHE_CSRF--&gt;` a nikdy nesedne bajt po bajtu se značkou v neescapované atributové hodnotě formuláře
- 2026-08-03: **`mayStore()` odmítá jen na `Cache-Control: no-store`, nikdy na `private` (kritický nález review, vlna 3.0, odchylka od plánu).** Symfony/Laravel razítkuje `no-cache, private` na každou odpověď se session cookie jako framework default — kontrola na `private`, jak psal původní plán, by neuložila vůbec nic a celá vlna by byla mrtvá už při prvním requestu. Kontrolu na osobní obsah dělá `PageCachePolicy::tenantFor()` dřív (odmítne každý request se session state), takže `mayStore()` smí kontrolovat jen `no-store`, kterým routy (košík, pokladna, `/api/kosik/souhrn`) explicitně opt-outují
- 2026-08-03: **`TenantContext::set()` při stejném id tenanta jen vymění svázanou instanci, nespouští znovu celý `makeCurrent()` pipeline (mimo deklarovaný rozsah vlny 3.0, ponecháno na rozhodnutí vlastníka).** `spatie/laravel-multitenancy`'s `makeCurrent()` má krátký obvod (`isCurrent()`, klíčovaný jen na primárním klíči) — worker, který přežije víc než jeden request (test suite dnes, Octane případně později), by tak druhý request pro stejného nájemce potichu servíroval atributy z prvního requestu. Objeveno přes `page_gen_*` čítače: bump aktualizoval DB řádek správně, ale `TenantContext::current()` to nikdy neviděl. Spuštění celého switch-task pipeline pokaždé není řešení — `PrefixCacheTask` zapomíná cache driver při každém přepnutí, což u `array` driveru udělá z každého requestu cache miss
- 2026-08-03: **Odpis skladu zvedá čítač jen na hranici skladem/vyprodáno, a bump je odložený přes `DB::afterCommit`, ne inline (vlna 3.0).** Naivní bump při každé objednávce by na rušném e-shopu cache nikdy netrefila — detail produktu zobrazuje dostupnost, ne počet kusů. Inline bump navíc drží zámek na řádku `tenants` po zbytek `OrderPlacer`'s transakce; dvě souběžné objednávky, z nichž každá vyprodá jiný produkt a nese ten druhý jako druhou položku, by se mohly zaklínit navzájem (produktový zámek jedné proti `tenants` zámku druhé) — `OrderPlacer` takový deadlock nechytá, skončilo by to jako nezachycená `QueryException` místo čisté objednávky. Restock má zrcadlový bump (`bumpIfRestocked`) ze stejného důvodu v opačném směru
- 2026-08-04: **Fold hledaného termínu má jedinou definici (`PageCacheKey::foldSearchTerm()`), použitou v klíči, ve storefront hlavičce i na `/hledani` (vlna 3.0, kritický nález review).** Klíč zprvu jen trimoval `q`, zatímco hledání samo folduje case i diakritiku (`SearchText::normalise()`) — každý pravopisný variant jednoho reálného termínu dostával vlastní cache záznam s identickým HTML. Fold nesmí být **agresivnější** než `SearchText::normalise()` (jinak dva termíny, které hledání rozlišuje, spadnou na jeden klíč a jeden návštěvník uvidí výsledky druhého) — tahle vazba kříží modulovou hranici, kterou typový systém nezkontroluje (`app/Core` nesmí importovat `Modules\Products`), takže je vynucená jen komentářem a testy
- 2026-08-05: **404 na neexistující cestě necachuje middleware routy — cachuje se výsledek hledání v `redirects` (vlna 3.0, odchylka od plánu).** Když nesedne žádná routa, routing vyhodí výjimku dřív, než doběhne k jakémukoli middlewaru na routě, takže page cache takovou cestu nikdy neuvidí — přesně tyhle URL buší skenery. `RedirectResponder` (handler `NotFoundHttpException`) proto cachuje sám sebe, klíčovaný na `Dimension::Catalog` (redirect vzniká přejmenováním slugu), ve stejném store a TTL jako `pagecache.ttl.not_found`

- 2026-08-05: **`TenantContext::set()` je jediná cesta přepnutí nájemce; `runAs()` a `runWithoutTenant()` už nevolají `makeCurrent()` (vlna 3.1, uzavření háčku 3.0).** Obezlička kolem short-circuitu `spatie/laravel-multitenancy` seděla jen v `set()`, kdežto obě `run*` metody přepínaly napřímo — díra tedy zůstala otevřená pro **každého** volajícího, který přes ně přepíná: superadmin změna stavu, oba Stripe handlery, lifecycle sweeper, `TenantProvisioner` a každý zápis do `AuditLog`. Předaný čerstvě načtený model téhož nájemce se nesvázal (`isCurrent()` je klíčovaný jen na primárním klíči) a callback četl atributy staré instance. Pouštět celý switch-task pipeline pokaždé řešení není — `PrefixCacheTask` zapomíná cache driver při každém přepnutí, což u `array` driveru udělá z každého requestu cache miss
- 2026-08-05: **`ShippingMethod` invaliduje `Dimension::Catalog` (vlna 3.1).** Oba feedy tisknou blok `DELIVERY` per způsob dopravy a klíč feedu nese razítko `Catalog`, ale model chyběl v mapě `PageCacheObserver` — přejmenovaný, zdražený nebo vypnutý dopravce zůstal v Heurece až hodinu, tedy cena dopravy, kterou e-shop neúčtuje. `Catalog` je široká dimenze na jednoho dopravce (bump vezme i produktové stránky, sitemap a druhý feed), ale doprava se mění řádově jednou za měsíce — na rozdíl od nastavení feedu, které nájemce ladí v sériích a které proto má cílený `Cache::forget` ve `FeedAdminController`. `PaymentMethod` se vědomě nepřidává: žádný feed ho nenese a jediná stránka, kde se objevuje, je pokladna (`no-store`)
- 2026-08-05: **Pošta o změně stavu e-shopu visí na doménovém eventu z `changeStatus()`, ne na volajících (vlna 3.1).** Volajících je dnes pět a přibývají, takže instrumentovat je po jednom znamená, že šestý se zapomene — stejná úvaha, proč page cache jede na observeru a ne ve writerech. `TenantStatusChanged` nese `from` **i** `to`, protože stejný cílový stav znamená různou věc: `trial→past_due` je „skončil trial", cokoli jiného`→past_due` je „platba selhala". Dispatch je odložený přes `DB::afterCommit` — `StripeWebhookHandler` mění stav uvnitř transakce nesoucí idempotenční claim, takže inline odeslání by informovalo o změně, která se pak vrátila zpět. `SweepTenantLifecycle` svá dvě vlastní odeslání **ztratil** (jinak by trial→past_due poslal dvakrát). Vždy `MailKind::Transactional`: zpráva o nezaplacené faktuře nesmí být to, co nezaplacená faktura zablokuje. `trial→active` a `→deleted` mlčí schválně — první platbu potvrzuje Stripe vlastní účtenkou plus český doklad, a po `pending_deletion` už není komu psát
- 2026-08-08: **Kategorie admin menu drží jádro, manifest říká jen `group` (vlna 3.5).** Menu se dál staví z manifestů modulů — natvrdo napsaný seznam v layoutu by znamenal, že vypnutý modul nechá viset odkaz do 404. Kategorie a jejich **pořadí** ale patří jádru (`NavigationGroup`): menu řazené podle toho, který modul se nainstaloval dřív, by se přeskládalo při každém zapnutí. Vadná skupina padá při `modules:sync`, ne až při requestu — překlep by položku zařadil do záložní sekce, kde vypadá **zařazeně, ne špatně zařazeně**, a nikdo nehledá položku, která je vidět, jen jinde. Prázdná kategorie se nevrací (nadpis bez obsahu čte jako rozbité). Jádrové položky (Doména, Vzhled, Nastavení modulů) přidává layout, ne builder: nepatří žádnému modulu a builder nemá znát routy mimo modulový systém
- 2026-08-08: **Zavřený mobilní drawer je `invisible`, ne jen posunutý mimo obrazovku (vlna 3.5, nález E2E).** `-translate-x-full` menu odsune z pohledu, ale **nechá ho v pořadí tabulátoru a odečítač ho dál čte** — uživatel klávesnice na telefonu by procházel menu, které nevidí. `visibility` je animovatelná, takže vysunutí zůstalo plynulé. Ve sbaleném railu se naopak nadpisy sekcí nevykreslují a **všechny položky jsou vidět**: schovat je za sekci, kterou v railu nelze otevřít, by polovinu administrace znepřístupnilo. Sbalení je vázané na `matchMedia` — kdo si menu sbalí na notebooku, nesmí na telefonu najít menu, které nejde přečíst. `localStorage` se čte i zapisuje v `try/catch`: administrace, která se neotevře v anonymním okně, je horší než ta, která zapomene rozbalené sekce
- 2026-08-08: **`Route::has()` nestačí jako gate na modulový odkaz (vlna 3.5, nález testu).** `ModuleRouteRegistrar` montuje routy **všech nasazených** modulů bez ohledu na to, kdo co zapnul, takže `Route::has('admin.orders.index')` je pravda i pro e-shop, který objednávky nikdy nezapnul — a odkaz by vedl na 404 z modulové brány. Nástěnka se proto ptá `ModuleRegistry::isEnabled()`. Sama nástěnka je nová: `/admin` dosud přesměrovával na první položku menu, takže vlastník přistál na výpisu produktů bez ponětí, jak si e-shop stojí. `OrderBook::dashboardSummary()` počítá čtyři agregáty jedním dotazem — první obrazovka administrace nesmí být ta nejdražší
- 2026-08-08: **Superadmin dostal stejné rozvržení, ale nechal si tmavý panel (vlna 3.5).** Vlastník chtěl sjednotit obojí; sjednoceno je **rozvržení**, ne barva. Záměna platformní konzole s administrací nájemce je způsob, jak pozastavit špatný e-shop. Superadmin nemá kategorie — šest obrazovek je nepotřebuje
- 2026-08-06: **E2E sada nepotřebuje zvláštní doménu ani certifikát (vlna 3.4, obě premisy vlny vyvráceny).** V `STATUS.md` od vlny 1.x viselo, že Playwright blokuje omezení wildcard certifikátu — týká se to `curl` přes **HTTPS**, kdežto sada mluví prostým HTTP proti `artisan serve`, kde žádné TLS není. Plán proto stavěl na samostatné `droidshop.test` (RFC 6761) s tím, že Chromium doménu bez TLD upgraduje na HTTPS nebo pošle do vyhledávače; **ověřeno, že u explicitní `http://` URL s portem to nedělá**. Sada jede na `obchod.droidshop`, tedy na téže doméně jako demo i PHPUnit sada, a nevyžaduje sudo-editovaný `/etc/hosts` na každém stroji. `E2E_HOST` zůstává jako úniková cesta
- 2026-08-06: **E2E má vlastní databázi `droidshop_e2e` a vlastní port 8001 (vlna 3.4).** `migrate:fresh` na lokální databázi by smazal data, která si vlastník proklikal v demu — sada, která kvůli svému běhu maže cizí data, je horší než žádná; databázi si zakládá sama. Port 8001 (demo jede na 8000) znamená, že sada nezávisí na tom, jestli někomu běží server, a ani ho nezabije. **Page cache je pro E2E vypnutá** (`PAGE_CACHE_ENABLED=false`): jinak by scénář dostal HTML uložené tím předchozím a první červený test by byl nevysvětlitelný; cache má vlastních 108 PHPUnit testů. **Požadavky na měřicí vendory se odchytávají, nepouštějí ven** — testuje se pokus o požadavek (přesně to, co řeší ePrivacy) a CI nesmí záviset na dostupnosti Googlu
- 2026-08-06: **Test, o kterém nevíme, že umí selhat, negarantuje nic (vlna 3.4).** Gate souhlasu v `tracking.blade.php` byl dočasně porušen, aby se ověřilo, že sada zčervená. Zčervenala — ale **jiné testy, než plán čekal**: nosné jsou scénáře **odmítnutí**, kdežto „nic před rozhodnutím" zůstal zelený, protože bez zaznamenaného rozhodnutí se snippet nespustí vůbec a gate uvnitř nemá šanci být špatně. Zapsáno přímo u testu. Totéž u přístupnosti: `axe-sanity.spec.ts` podstrčí obrázek bez `alt` a trvá na tom, že ho audit chytí — zelený a slepý audit jsou zvenčí k nerozeznání. Axe blokuje jen `critical`/`serious`, protože sada, která zčervená na neopravovaném nálezu, se začne přeskakovat i s nálezem, který je skutečný
- 2026-08-06: **Bez JavaScriptu je checkout o krok delší, a je to v pořádku (nález vlny 3.4).** Platební metody se zobrazí až po odeslání dopravy, protože nabídka závisí na zvoleném dopravci (matice doprava×platba). PHP testy to neviděly, protože posílají oba údaje najednou. Lišta cookies navíc bez JS po rozhodnutí **nezmizí** — je v cachovaném HTML a skrývá ji skript — což je vědomé chování z 3.3; test proto ověřuje, že se rozhodnutí uložilo, ne že lišta zmizela. Bez JS stejně neběží žádné měření, takže lišta nic neblokuje
- 2026-08-05: **Server renderuje konfiguraci měření, nikdy rozhodnutí návštěvníka (vlna 3.3).** Měřicí id jsou per tenant — stejná pro každého — takže smějí do cachovaného HTML; souhlas je per návštěvník a nesmí. Blade `@if` na souhlasu by z cachované stránky udělal roznašeč cizího rozhodnutí (tatáž třída chyby, kvůli které 3.0 zrušila cookie `has_cart`). Server proto vloží jen `<script type="application/json" id="tracking-config">` s id a JS podle cookie rozhodne, co spustit; test asertuje, že HTML je **byte-identické** pro nerozhodnutého, souhlasícího i odmítajícího. `PageCachePolicy` se nemění a cookie souhlasu nezpůsobí miss
- 2026-08-05: **GA4 se nenačítá s Consent Mode „denied", ačkoli to Google doporučuje (vlna 3.3).** Consent Mode v2 dovoluje načíst `gtag.js` se vším zamítnutým, ale **požadavek stejně dorazí ke Googlu a nese IP adresu návštěvníka** — a právě požadavek před souhlasem je to, co ePrivacy zakazuje, bez ohledu na to, jestli se vrátí cookie. Volání `consent default denied` přesto běží první, protože bez něj se od 2024 GA4 v EU nespáruje s Google Ads. Sklik a Meta Consent Mode nemají, takže se jejich skript vkládá až po souhlasu. Test na „nic nesmí volat cizí doménu" asertuje na atributy `src`/`href`, ne na výskyt hostname kdekoli v HTML: loader nese URL jako řetězce uvnitř funkcí gatovaných cookie a přepisovat je kvůli substring testu by byla obfuskace, ne záruka
- 2026-08-05: **Cookie lišta patří do jádra, měřicí kódy do modulu (vlna 3.3).** Lišta je právní povinnost každého e-shopu, i toho, který nic neměří — modul, který jde vypnout, ji držet nemůže. `app/Core/Consent/` proto čte souhlas a renderuje lištu, modul `analytics` (base, stejný argument jako `feeds`) na souhlas jen reaguje. Cookie je **vyloučená ze šifrování a není httpOnly**, protože ji čte storefront JS, aby lištu skryl před prvním vykreslením; nese tři booleany a verzi, takže podvržené „souhlasím" jen opt-inuje toho samého návštěvníka. Tlačítka „Přijmout vše" a „Odmítnout vše" mají **shodné třídy** a hlídá to test — nerovnocenná volba znamená, že souhlas není svobodný (EDPB 03/2022), a okem se to v review neuhlídá
- 2026-08-05: **`SettingsField` umí `secret` (vlna 3.3).** Tabulka `settings` ukládá prostý JSON, ale Heureka API klíč je credential podle §16.5. Příznak znamená: šifrování při zápisu, nikdy se nevrací do administrace (jen `{key}_stored` boolean) a **prázdné pole při uložení = neměnit** (keep-on-update jako Comgate a Packeta). Dešifrování selhává **do prázdna, ne do ciphertextu** — hodnota po rotaci `APP_KEY` by se jinak odeslala třetí straně, jako by to byl klíč. Odhaleno testem: `SettingsSchema::fields()` je `array_values()`d, takže `array_keys(array_filter(...))` vracelo pozice místo názvů polí a šifrování tiše nefungovalo
- 2026-08-05: **Heureka Ověřeno zákazníky stojí mimo souhlasový režim (vlna 3.3).** Neukládá nic do prohlížeče, takže § 89 odst. 3 se neuplatní a právním titulem je oprávněný zájem nájemce. Zákazník o tom ale vědět musí, takže vzor zásad ochrany údajů z 3.2 dostal odstavec, který službu i titul jmenuje. Volání je server-to-server, ne widget: e-mail zákazníka neprochází stránkou a odeslání přežije zavření záložky. Visí na doménovém eventu modulu `orders` (resolvovaném **stringem** — `analytics` nedeklaruje `requires` na `orders`), takže dotazník dostane i ručně založená objednávka. Selhání Heureky jen loguje — běží po commitu objednávky, takže nejhorší, co může udělat, je nic
- 2026-08-05: **Právní stránky platformy sedí pod `/pravni/*`, ne na kořeni (vlna 3.2).** Prefix není kosmetika: od 3.1 obsluhuje `/{slug}` na hostu nájemce `Route::fallback()`, takže jednosegmentová platformní routa `/cookies` by se namatchla i tam — a `RequirePlatformHost` odpoví 404 **až po** matchi, takže fallback by se neuplatnil a stránka nájemce by zmizela. `Modules\Pages\Lifecycle` navíc seeduje `ochrana-osobnich-udaju` každému e-shopu, takže by šlo o jistotu, ne riziko. Dvousegmentová cesta kolizi vylučuje konstrukcí (stránky nájemce jsou vždy jednosegmentové). Alternativa `Route::domain(config('tenancy.platform_domain'))` neprošla: doména se do routy zapeče při bootu, kdežto testy nastavují `tenancy.platform_domain` až v `setUp()` — `isPlatformHost()` čte config za běhu, a proto middleware cesta funguje. Slug se mapuje na view přes server-side allowlist, nikdy na cestu k souboru
- 2026-08-05: **Souhlas s VOP nese datum i verzi, obojí nebo nic (vlna 3.2).** `users.terms_accepted_at` + `users.terms_version` (řetězec z `config('legal.terms_version')`, datum): časové razítko samo neřekne, jaké znění nájemce viděl, verze sama neřekne kdy. Validuje se **server-side** (`accepted` v `RegisterRequest`), ne jen atributem `required` na checkboxu — důkaz, který klient může přeskočit, není důkaz. Pole musela přibýt do `User::$fillable`; bez toho mass assignment zápis tiše zahodil (odhalil test, ne review). Historie znění je v gitu, ne v tabulce verzí: prokázat *znění* k datu by chtělo vlastní tabulku, což je mimo rozsah
- 2026-08-05: **Vzory právních stránek nájemce jdou publikovat i nedoplněné, jen s varováním (vlna 3.2).** Tvrdá blokace publikace stránky s `[DOPLŇTE …]` zvážena a zamítnuta — nájemce může mít legitimní důvod publikovat rozpracovaný text a administrace, která odmítne uložit, působí jako chyba. Místo toho: po seedu jsou stránky nepublikované, seznam nese trvalé upozornění o odpovědnosti a formulář varuje v okamžiku zaškrtnutí publikace. Vzory jsou testované proti `HtmlSanitizer` — značka mimo allowlist by se odstranila při prvním uložení a vzor by tiše ztratil strukturu. **Editor stránek doplněn v téže vlně:** modul `pages` byl read-only, takže bez něj by vzory zůstaly navždy nedoplněné a celá právní vlna by byla inertní
- 2026-08-05: **Publikované stránky sdílí modul `pages` dvěma views, ne layout composer storefrontu (vlna 3.2).** Composer na `storefront::layouts.shop` nikdy nedosáhne do `checkout/details`, protože **Blade child se renderuje před svým layoutem** — a pokladna je jediné místo, kde zákazník výslovně souhlasí s podmínkami, takže tam odkaz chybět nesmí. Odhaleno falešně prošlým testem: patička na téže stránce nese stejné URL, takže assert na `href` matchoval ji; teprve zpřísnění na text odkazu (patička „Obchodní podmínky", souhlas skloňované „obchodními podmínkami") ukázalo pravdu. Data proto sdílí modul, který vlastní model. Nepublikovaná stránka zůstává prostý text — odkaz do 404 tam, kde zákazník s něčím souhlasí, je horší než žádný odkaz
- 2026-08-05: **`Page` nepřepisuje `getRouteKeyName()` (vlna 3.2).** Vracel `'slug'` kvůli storefront routě, která od vlny 3.1 model vůbec neváže (`PageController` čte cestu z requestu). Admin je jediný, kdo `Page` váže, a vazba podle slugu by znamenala, že se URL editace mění při přejmenování — otevřená záložka by po vlastním uložení 404ovala
- 2026-08-05: **Přepis vyhledávání na FULLTEXT zrušen po měření, ne po odhadu.** `LIKE '%term%'` nad `products.search_text` běží **3,2 ms** na base tarifu a **31 ms** na stropu premium (5000 produktů): index na `search_text` se opravdu nepoužije, ale `tenant_id` scope zúží scan na katalog jednoho nájemce a víc než 5000 produktů tarif nedovolí. Výsledek hledání se navíc od 3.0 cachuje. FULLTEXT by přinesl regresi kvality (hledá celá slova, ne substring — „vesnic" by přestalo najít „klávesnice"; `innodb_ft_min_token_size` je 3, takže „TV" by vypadlo) za úsporu, kterou nikdo nepozná. Argument „fulltext nejede na SQLite v testech" z rozhodnutí 2026-07-20 mezitím taky odpadl — testy jedou na MySQL. Vrátit se k tomu, až vznikne tarif nad 5000 produktů
- 2026-08-05: **Statické stránky sedí na `/{page-slug}` přes `Route::fallback()`, ne přes catch-all `/{slug}` ani blacklist rezervovaných segmentů (vlna 3.1, uzavření odchylky od pravidla storefrontu).** Laravel vyhodnocuje fallback až po všech ostatních routách bez ohledu na pořadí registrace — a to je load-bearing: `ModuleRouteRegistrar` iteruje `glob()` abecedně, takže `pages` se registruje **před** `products` i `storefront` a catch-all by opravdu spolkl `/kosik`, `/hledani` a zbytek. Blacklist by musel někdo ručně rozšiřovat při každé nové routě a chyběl by tiše. Cena fallbacku je, že matchuje i víceúrovňové cesty a `/admin/*`, takže controller odmítne cokoli s lomítkem a `abort(404)` — čímž request předá `RedirectResponder`u a přejmenované slugy dál 301ují. Stará `/stranka/{slug}` odpovídá 301 a **nedotazuje databázi**: redirect platí i pro slug, který nikdy neexistoval, a dotaz by z routy udělal oracle na to, které stránky nájemce má

- 2026-08-08: **Nastavení obchodu má vlastní tabulku `shop_settings`, ne `settings`, `tenants` ani `tenant_theme` (vlna 3.6).** `SettingsService` z 2.10 klíčuje **podle modulu** a validuje proti manifestu, takže by se muselo vymyslet, který modul „vlastní" časové pásmo; `tenants` je platformní záznam o zákazníkovi (kdo je, co platí, jestli je pozastavený), `tenant_theme` je branding čtený view composerem na každém storefront requestu a rozšířit ho znamená vléct tucet sloupců, na které se nikdo nedívá, do toho čtení. Čtení nikdy nevrací `null` — nájemce bez řádku dostane neuloženou instanci s výchozími hodnotami, stejná úvaha jako `SettingsService::all()` slévající defaulty: výchozí hodnoty jsou jediná pravda o tom, na čem běží nedotčený e-shop, ne `?? '…'` roztroušené po volajících. Zápis zvedá **všechny tři** generace page cache, tupěji než observer mapující model na jednu dimenzi: slogan je téma, patička obsah, skrývání kategorií katalog — dělit zápis podle pole znamená, že příští přidané pole dostane špatnou dimenzi, a nastavení se mění párkrát za život e-shopu
- 2026-08-08: **`noindex` e-shopu jde do meta tagu i do `robots.txt` a s přepínačem stránky se slučuje, nikdy nepřepisuje (vlna 3.6).** Jedno bez druhého je poloviční zákaz: crawler, který stránku nestáhne, meta tag nikdy nepřečte, a crawler, který `robots.txt` ignoruje, tag přečte. Slučování (OR) drží košík, pokladnu a děkovnou stránku `noindex` bez ohledu na to, co říká nastavení e-shopu. Prázdný titulek a popis **degradují na dosavadní chování** (název obchodu), ne na prázdný `<title>` — ten je horší než automatický, který nahradil. Obrázek pro sdílení je raster-only bez SVG, stejná politika jako favicon (2.2) a obrázky produktů
- 2026-08-08: **Skrývání prázdných kategorií se ptá na celou větev, ne na vlastní produkty kořene (vlna 3.6).** Běžný katalog má všechno v listech („Nářadí > Vrtačky") a kořeny prázdné záměrně; počítat jen produkty přímo v kořeni by přepínačem smazalo celé horní menu a nájemce by si myslel, že mu smazal kategorie. E-shop bez jediného publikovaného produktu si menu nechá **celé** — schovat všechno vypadá jako rozbitý e-shop, ne prázdný. Mřížka kategorií na homepage se nedotýká: to je obsah, který si nájemce vybral v page builderu (2.3), ne navigace
- 2026-08-08: **Vlajka zaheslování je zrcadlená na `tenants.storefront_locked`, autorita nastavení i hash hesla zůstávají v `shop_settings` (vlna 3.6).** `EnsureShopUnlocked` i `PageCachePolicy` se ptají „je tenhle e-shop zamčený" na **každém** requestu, včetně těch, které by teplá page cache odbavila bez jediného dotazu; čtení ze `shop_settings` stálo dva dotazy na cache hit a shodilo rozpočet dotazů, který si vlna 3.0 nastavila právě proto. Stejný argument, který tam dal generační čítače: `DomainTenantFinder` ten řádek stejně načítá, takže sloupec na něm je zdarma. `ShopSettingsService` je jediný zapisovatel a drží obojí v souladu (hlídá test) — přímý zápis do modelu by je rozešel a e-shop by na obrazovce tvrdil, že je zamčený, zatímco v prohlížeči je otevřený
- 2026-08-08: **Zámek storefrontu sedí ve `web` skupině před page cache; webhooky, administrace a `robots.txt` jsou mimo něj (vlna 3.6).** Middleware appendnutý do `web` běží za `StartSession` (odemčení drží session, ne ruční cookie — session je už podepsaná a šifrovaná, vlastní cookie by potřebovala vlastní HMAC) a **před** každým route middlewarem včetně `page-cache`, takže odmítnutý požadavek se k cache vůbec nedostane a stránka uložená před zamčením se po něm neservíruje. Zamčený e-shop, který přestane přijímat notifikaci „objednávka je zaplacená", ztratí platbu tiše a nájemce se to dozví od zákazníka — proto jsou callbacky brány i dopravce v allowlistu. Odemčení je klíčované id tenanta: e-shopy na subdoménách platformy sdílejí doménu cookie a odemčení jednoho nesmí odemknout druhý. Pokusy jsou omezené počtem (vzor `ApplyDiscountRequest` z 2.6) — ručně psané heslo je hádatelné z definice. **Odchylka od specifikace:** sitemap a feedy jsou pod zámkem (spec je nechávala venku) — veřejná sitemap zamčeného e-shopu vydává seznam URL katalogu, který zámek zbytek času tají

## Před spuštěním (právní / provozní)
- [x] VOP platformy (odpovědnost nájemce za obsah) — **draft hotový** (vlna 3.2), čeká na právní revizi
- [x] GDPR / zpracování osobních údajů (platforma + vzor pro nájemce) — **draft hotový** vč. zpracovatelské smlouvy podle čl. 28
- [ ] **Právní revize draftů** — začít od markerů `> **K PRÁVNÍ REVIZI:**` v `docs/legal/` (limitace náhrady škody, výpovědní doba, SLA, lhůta pro ohlášení porušení zabezpečení, rozsah auditního práva)
- [ ] `.env` produkce: `BILLING_COMPANY_ICO`, `BILLING_COMPANY_ADDRESS`, `BILLING_COMPANY_EMAIL` — bez nich se právní stránky vyrenderují s prázdnými identifikačními údaji
- [ ] Rozhodnout, zda nájemcům registrovaným před vlnou 3.2 (bez `terms_accepted_at`) předložit souhlas dodatečně
- [x] Cookies / ePrivacy — zásady platformy (3.2) i cookie lišta se třemi kategoriemi a měřicí kódy nájemce (3.3) hotové
- [ ] **Vlna 3.3 — měření:** ověřit s **reálnými** účty GA4 (přijde `page_view` i `purchase`), Sklik konverzi, Meta Pixel a Heureka API klíč — testy je stubují. Projít lištu se zapnutým blokátorem reklam (může zablokovat i náš vlastní loader). `modules:sync` před `migrate` (nový modul `analytics`)
- [ ] Platební účet platformy (předplatné)
- [ ] Wildcard DNS + TLS `*.droidshop.cz` (nebo finální doména)
- [ ] Stripe Billing Portal nakonfigurovat: povolit „switch plans" se 4 cenami (base/premium × month/year), proration = „Prorate changes"; configuration id do `config('billing.stripe.portal_config')`
- [ ] 4 Stripe Price objekty vytvořit a jejich id zapsat do `plan_prices`; povolit event `customer.subscription.updated` na webhook endpointu
- [ ] **Vlna 2.11/2.12 — účetní export:** ověřit **reálný import Pohoda XML do Pohody** (ISDOC už hlídá test proti oficiálnímu XSD, neplátce DPH je otestovaný); po nasazení zkontrolovat, že fakturační profily nájemců mají vyplněnou zemi
- [ ] **Vlna 3.6:** `php artisan migrate` (`shop_settings`, `tenants.storefront_locked`) + `npm run build`; po nasazení projít čtyři obrazovky nastavení a ověřit, že se slogan a kontakty objeví na storefrontu
- [ ] **Vlna 2.10:** `php artisan modules:sync` **před** `migrate` (nová manifestová pole a schémata; sync odmítne vadné schéma), pak `migrate` (přesun `variant_display` + drop sloupce) a `npm run build`
- [ ] **Vlna 2.1 — custom domény:** Caddy `on_demand_tls { ask http://127.0.0.1:<port>/internal/tls-check?token=<PLATFORM_TLS_CHECK_TOKEN> }`; on-demand jen pro custom domény, subdomény wildcard cert (DNS-01); Caddyfile **zamítni veřejný `/internal/*`**
- [ ] `edge.droidshop.cz` A → VPS IP (cíl CNAME custom domén nájemců)
- [ ] `.env` produkce: `PLATFORM_SERVER_IP`, `PLATFORM_EDGE_HOST`, silný `PLATFORM_TLS_CHECK_TOKEN` (stejný v Caddyfile ask URL)
- [ ] Cron `schedule:run` běží (kvůli `domains:sweep-pending` hodinově a `packeta:sync-points` denně) — runbooky `docs/as-is/2026-07-23-custom-domains.md` a `docs/as-is/2026-07-27-zasilkovna.md`
- [ ] **Vlna 2.5 — Zásilkovna:** `PACKETA_FEED_API_KEY` v produkčním `.env` (bez něj sync jede na klíči prvního nakonfigurovaného tenanta); ověřit reálná volání Packeta API s testovacím účtem (v testech `Http::fake`); zkontrolovat, že `PACKETA_TIMEOUT` je aspoň 2× menší než `submit_stale_after_minutes` (guard jinak selže hlasitě); ověřit, že widget v6 nenačítá cizí doménu dřív, než na něj zákazník klikne

## Údržba tohoto souboru
- Aktualizuj po strukturální změně, novém pravidle nebo rozhodnutí
- Detaily patří do `docs/` a kódu, ne sem
