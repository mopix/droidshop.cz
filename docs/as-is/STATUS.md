# As-is status — DroidShop.cz

Poslední aktualizace: **2026-08-12** · Verze: **0.46.0** (Zásilkovna — doručení na adresu)

## Oblasti

| Oblast | Stav | Spec | Poznámka |
|--------|------|------|----------|
| Laravel skeleton (Breeze + Inertia) | hotovo | — | výchozí app |
| AI / docs workflow | hotovo | bootstrap | `claude-laravel-vue` + WooShop struktura |
| Multi-tenancy — jádro | **hotovo** | §4.2, §4.3, §15.2 | [detail](2026-07-19-tenancy-jadro.md) |
| Izolace dat + CI brána | **hotovo** | §4.2 pojistky 1–4 | pojistka 4 (per-tenant export) doplněna vlnou 0.48 — [detail](2026-09-03-export-a-odinstalace.md) |
| Audit log | **hotovo** | §15.1 | e-mail o změně stavu chybí |
| Kernel služby — Money, Settings, Limits, Sequences, FeatureFlags | **hotovo** | §15.1 | [detail](2026-07-19-kernel-sluzby.md) |
| Kernel služba — FileStorage | **hotovo** | §15.1 | [detail](2026-07-19-filestorage.md); lokální disk, ne S3 |
| Kernel služba — MailService | **hotovo** | §15.1 | [detail](../superpowers/plans/2026-07-20-faze-1-vlna-13-etapa-1-mailservice.md); šablony verifikace a reset hesla dodal modul `customers`, potvrzení objednávky a stavové e-maily dodal modul `orders` |
| Kernel služba — EventBus | odloženo | §15.1 | čeká na prvního volajícího |
| Module system | **hotovo** | kap. 5, §15.5 | [detail](2026-07-19-system-modulu.md); odinstalace doplněna vlnou 0.48 jako **opt-in per modul** (`discounts`, `feeds`) — modul se zákonnou evidencí ji nemá a mít nesmí |
| Modul `pages` — statické stránky + editor | **hotovo** | — | Blade SSR na `/{slug}` (vlna 3.1); editor doplněn vlnou 3.2 — do té doby byla obrazovka read-only, takže nájemce nemohl své VOP vůbec publikovat. `PageWriter` (sanitizace při zápisu, unikátní slug, 301 při přejmenování), CRUD, vzory s `[DOPLŇTE …]`. Bez WYSIWYG — HTML v textarea |
| Právní minimum platformy | **hotovo** | kap. 11 | [detail](2026-08-05-pravni-minimum.md); vlna 3.2. Čtyři dokumenty v `docs/legal/` + renderované na `/pravni/*` (VOP, zásady zpracování = my správce vůči nájemci, DPA čl. 28 = my zpracovatel vůči jeho zákazníkům, cookies); prefix `/pravni/` je **nutný** — jednosegmentová platformní routa by na hostu nájemce zastínila jeho stránku, protože `RequirePlatformHost` 404uje až po matchi a fallback z 3.1 by se neuplatnil. Souhlas při registraci se zapisuje s datem i verzí (`users.terms_accepted_at`/`terms_version`), validace server-side. Nový e-shop dostane tři vzory místo prázdna; `footerPages` sdílí modul `pages` dvěma views, protože Blade child se renderuje **před** layoutem a composer na layoutu do pokladny nedosáhne. **Drafty bez právní revize** (rozhodnutí vlastníka) — markery `K PRÁVNÍ REVIZI` u limitace škody, SLA, lhůty ohlášení porušení a rozsahu auditu |
| Superadmin auth / `platform_admins` / 2FA / impersonace | **hotovo** | §15.4, §6.12 | [detail](2026-07-19-superadmin-auth.md) |
| Superadmin management UI — tenanti, stavy, tarify, moduly, kill switch | **hotovo** | §6.12, §15.5 | [detail](2026-07-20-superadmin-ui.md); bez metrik a bez zakládání tenantů |
| Zásilkovna — doručení na adresu | **hotovo** | 2026-08-11 | [detail](2026-08-11-packeta-home-delivery.md); vlna 2.5 uměla jen výdejní místa, takže zákazník, který chtěl zásilku domů, ji u žádného e-shopu nedostal. Nový driver `PacketaHomeDelivery` (`packeta_hd`) vedle stávajícího, oba v modulu `packeta`; `createPacket` s id partnerského dopravce místo id místa, pak `packetCourierNumber` (**součást podání**, ne tisku — štítek bez čísla kurýra neexistuje) a štítek přes `packetCourierLabelPdf`. Rozpojena tři místa přidrátovaná k Zásilkovně, čímž je konečně splněné AK §16.5. `provider`, `weight_grams` i `dimensions_mm` jsou na **top-level** snímku (vnořené čtení zůstává navždy jako záloha, žádná migrace snímků). **Nálezy:** objednávka na adresu se nedostala do expediční fronty (dotaz filtroval na `pickup_point->provider`, který u ní neexistuje) — našla to až závěrečná revize větve, protože všechny testy volaly službu přímo; a selhání mezi `createPacket` a `packetCourierNumber` nechávalo živý balík, který retry zdvojil (u dobírky zákazník platí dvakrát). Rozlišení Z-BOXů **vypuštěno** — jsou v jiném feedu, viz `docs/future/zasilkovna-z-boxy.md` |
| Rich text editor pro HTML pole administrace | **hotovo** | 2026-08-10 | [detail](2026-08-10-rich-text-editor.md); jedna sdílená komponenta `RichTextEditor.vue` (Tiptap 3.29) na popisu produktu, obsahu statické stránky a textovém bloku homepage — všechna tři pole byla holá `<textarea>`, kde nájemce psal `<h3>` ručně. Schéma je ruční zrcadlo `HtmlSanitizer::ALLOWED`: co toolbar nabídne, server neumaže, a co server povoluje, editor při načtení nezahodí (`Image` je proto ve schématu **bez tlačítka**). Server se nezměnil vůbec. Bez vkládání obrázků a bez zdrojového HTML (rozhodnutí vlastníka). Odkaz má vlastní dialog s kontrolou adresy přísnější než serverová. **Čtyři testy v této sadě byly odhaleny jako neschopné selhat** (uložení bez editace posílá netknutý Inertia prop, banner byl na obrazovce už před editací) — přepsané a s doloženým červeným během. Nález mimo rozsah: `HtmlSanitizer::isSafeUrl` propouští `//evil.com` (open redirect), zapsáno v `security_warnings.md` |
| Záložka Ceny + výpis produktů | **hotovo** | vlna 3.9 | [detail](2026-08-09-zalozka-ceny.md); **výpis** `/admin/m/products` nese sloupce Nákupní cena · Akční cena · (bez DPH · Daň) · Koncová cena — koncová je **pultová**, ne efektivní, a čistá cena se počítá z ní, ne přes `netPrice()` (ta vrací čistou z efektivní). Nákupní cena neopustí server bez `products.costs`. tři sekce (Prodejní cena, Nákupní cena, Akce), v každé pořadí **bez DPH → daň (sazba) → s DPH** — tak, jak o ceně nájemce přemýšlí. Nová **sleva v procentech**: procento se **ukládá**, takže zdražení nechá slevu dvacetiprocentní místo aby se tiše proměnila na dvanáctiprocentní; zadaná částka ale vyhrává a procento zahodí (jinak by se cena posouvala při každém uložení). Rozsah 1–99 % — sto procent je „zdarma", což je jiný nástroj. Nákupní cena dostala **vlastní sazbu** s dědičností („Stejná jako u prodeje"): dodavatel může účtovat jinak, než nájemce prodává, a přepočet prodejní sazbou hlásí tiše špatnou marži. Přepočet jde přes `ProductWriter`, takže platí zákonná evidence nejnižší ceny z 2.7 |
| Koruny v administraci, rozměry produktu, obrázky | **hotovo** | vlna 3.8 | [detail](2026-08-09-produkt-a-ceny.md); ceny se v celé administraci zadávají v **korunách** (produkt, varianty, akce, nákupní cena, doprava, platby, slevy, minimum objednávky) — sloupce zůstávají v haléřích, převod dělá `MoneyInput` v `prepareForValidation()`, protože `lt:price` by jinak porovnával koruny s haléři. Nikdy float na float (`(int)(0.07*100)` je 6); prázdné pole ≠ nula. Sleva se převádí **jen** u pevné částky, u procentní je `value` promile. Produkt dostal **rozměry** (mm, všechny tři nebo žádný): zákazník je vidí v novém bloku Parametry, dopravce je dostane při podání — ale jen když je zásilka jeden produkt, protože sečíst tři krabice nejde bez balicí logiky. Obrázky jde konečně **seřadit** (endpoint existoval od 1.2 bez ovládání) tlačítky, ne tažením; plocha na přetažení je doplněk. Uložit/Smazat produkt se přestalo kreslit nad panely obrázků a variant — proto vlastník přehlédl „Nastavit jako hlavní". **Dluh uzavřen 2026-08-09:** ono „padání `artisan serve`“ byla ve skutečnosti `https://` URL obrázků z `APP_URL` — Chromium mluvil TLS na HTTP server. Veřejné URL jsou nově relativní (produkční chyba: na vlastní doméně ukazovaly obrázky na platformní host) |
| Plátcovství DPH + čas obchodu | **hotovo** | vlna 3.7 | [detail](2026-08-09-dph-a-cas.md); `tenants.vat_payer` existoval od 1.5, ale nikdy se nedostal do katalogu — neplátce povinně vybíral sazbu, kterou nemá jak uplatnit, a jeho zákazníkům se tisklo „s DPH · bez DPH“, tedy nepravdivý údaj o cizím daňovém postavení. Nově jedno místo `VatMode`: neplátce nevidí DPH v administraci produktu, na detailu, ve výpisu ani v rozpisu pokladny; plátce beze změny plus **nové pole „cena bez DPH“** (převod na serveru přes `TaxRate`, nikdy v JS; při obojím rozhoduje cena s DPH). Sazby DPH konečně jde spravovat — `/superadmin/dph`, platformní (sazba je zákon), používanou sazbu nelze smazat, zápis invaliduje denní cache, změna procenta **nesahá na vystavené doklady**. Uložená sazba neplátci zůstává, nový produkt dostane výchozí. Zároveň uzavřen dluh 3.6: `ShopClock` používá nastavené pásmo a formát v administraci objednávek, účtu zákazníka a v PDF; `DATE` sloupce (DUZP, splatnost) se formátují, ale **neposouvají**; strojové formáty (ISDOC, Pohoda, feedy, sitemap) zůstávají `Y-m-d` |
| Nastavení obchodu (nájemce) | **hotovo** | vlna 3.6 | [detail](2026-08-08-nastaveni-obchodu.md); čtyři obrazovky pod NASTAVENÍ (Obchod, Kontakty, SEO, Zobrazení) + fakturační profil konečně v menu. Vlastní tabulka `shop_settings` — `tenants` je platformní záznam, `tenant_theme` branding a `settings` klíčuje podle modulu, takže by se muselo vymyslet, který modul „vlastní" časové pásmo. Čtení nikdy nevrací null (nájemce bez řádku dostane výchozí hodnoty), zápis zvedá všechny tři generace page cache. `noindex` jde do meta tagu **i** `robots.txt` a s přepínačem stránky se slučuje, nikdy nepřepisuje. Skrývání prázdných kategorií se ptá na celou větev (kořen s produkty jen v podkategorii zůstává) a e-shop bez publikovaných produktů si menu nechá celé. **Zaheslování e-shopu**: middleware ve `web` skupině před page cache, heslo hashované s omezením pokusů, webhooky a administrace mimo zámek; vlajka zrcadlená na `tenants`, protože čtení ze `shop_settings` stálo dva dotazy na každý cache hit. Formát data/času a časové pásmo se ukládají, ale zatím se nikde nepoužívají |
| Admin nájemce — layout, seskupené menu, nástěnka | **hotovo** | vlna 3.5 | [detail](2026-08-08-admin-layout.md); plná šířka, levé menu ve čtyřech kategoriích (kategorie a jejich pořadí drží jádro, manifest říká jen `group`), sbalitelné do ikon, na mobilu drawer. `/admin` má poprvé vlastní nástěnku (`OrderBook::dashboardSummary()` — čtyři agregáty jedním dotazem). Superadmin dostal totéž rozvržení, ale nechal si tmavý panel — záměna dvou administrací je způsob, jak pozastavit špatný e-shop. Zavřený drawer je `invisible`, ne jen posunutý mimo obrazovku: jinak zůstává v pořadí tabulátoru a odečítač ho čte |
| Admin nájemce — shell, navigace z modulů, oprávnění | **hotovo** | §15.4, §15.5 | [detail](2026-07-20-katalog-jadro.md) |
| Kernel — sazby DPH, redirects, sanitizace HTML | **hotovo** | §6.2, §15.3, §16.1 | [detail](2026-07-20-katalog-jadro.md) |
| Modul `categories` — strom, admin, 301 | **hotovo** | §6.3, §16.2 | max 4 úrovně; řazení tlačítky, ne drag&drop |
| Modul `products` — katalog, ceny, sklad, obrázky, SEO | **hotovo** | §6.2, §16.1 | bez CSV importu, řezů obrázků a hromadných operací |
| Modul `products` — varianty produktu (víceosá matice) | **hotovo** | vlna 2.4 | [detail](2026-07-26-varianty-produktu.md); 4 relační tabulky (`product_options`/`product_option_values`/`product_variants`/`product_variant_values`), core `CatalogVariant` + rozšíření `ProductCatalog` (`?int $variantId` poslední parametr, žádný callsite se nemění), server-authoritative resoluce (`option_value_id[]`→`resolveVariant()`), storefront picker radio/select bez JS + vanilla JS ostrůvek (`resources/js/storefront.js`, žádné Alpine), sklad/cena/objednávka snapshotují variantu; obrázky/URL/hmotnost per varianta, fasetový filtr a hromadný import variant = `docs/future/` |
| Modul `products` — akční ceny + nejnižší cena za 30 dní (Omnibus) | **hotovo** | vlna 2.7 | [detail](2026-07-28-akcni-ceny.md); `products.sale_price` + okno kampaně (`sale_starts_at`/`sale_ends_at`, okno jen na produktu), `product_variants.sale_price` s dědičným pravidlem, mrtvý `compare_at_price` zahozen. `ProductCatalog::price()` a `catalogPrice()` vracejí **efektivní** cenu, takže košík, objednávka, doklady i slevový engine 2.6 účtují akční cenu bez změny volajícího kódu; řazení katalogu přes `CASE WHEN`. Časová řada `product_price_history` zapisuje i **plánované budoucí** intervaly, takže konec akce nepotřebuje cron; uzavřený řádek se nikdy nemění. `LowestPriceCalculator` počítá okno **ke startu kampaně** (akce není součástí své vlastní reference), badge `−N %` se počítá z této reference. Storefront Blade SSR bez JS: akční + přeškrtnutá cena, povinný řádek na detailu. Zároveň uzavřen dluh 2.6: `tax_rate_id` poplatků povinné pro plátce DPH + backfill. Řádek nejnižší ceny ve výpisu kategorie, hromadné akce a filtr „ve slevě" = `docs/future/` |
| Modul `products` — CSV import a export katalogu | **hotovo** | vlna 2.8 | [detail](2026-07-28-csv-import.md); `ProductCsvSchema` je jediná pravda o formátu pro oba směry, takže export produkuje přesně to, co import přijme (hlídá `CsvRoundTripTest`). Import zakládá i aktualizuje **podle SKU** (prázdné SKU = nový, duplicitní = chyba řádku), umí varianty (`varianta_rodic_sku` + `Velikost:M|Barva:černá`, nový `VariantWriter::upsertVariant`), zařazuje do **existujících** kategorií (neexistující cesta = chyba řádku, strom se importem nemění) a respektuje limit tarifu (shodí jen svůj řádek). Zápis jde výhradně přes `ProductWriter`/`VariantWriter`, takže platí sanitizace HTML, unikátní slug, 301 i **zápis do historie ceny** z 2.7. Běh je queued job nad `product_imports`, jedna transakce na řádek, chybné řádky do protokolu ke stažení; přepínač „jen zkontrolovat" nezapíše nic. Export streamovaný, nákupní cena jen s `products.costs`, formula injection neutralizovaná. Obrázky z URL, mapování sloupců a mazání importem = `docs/future/csv-import-dalsi-kroky.md` |
| Doklady — doprava a poplatek na dokladu, validace ISDOC | **hotovo** | vlna 2.12 | [detail](2026-07-31-doprava-na-dokladu.md); faktura konečně **sečte**: `InvoiceSnapshot`/`ProformaSnapshot` nesou dopravu i poplatek za platbu jako řádky ve stejném tvaru jako položky (účtuje se `charged`, ne `price`; u neplátce sazba 0, ne default; nulový řádek se zobrazuje). Historické doklady beze změny, export si u nich dopočte zbytek. Uzavírá dluh 1 a 2 z vlny 2.11 vč. tichého vynechání dopravy u **neplátce DPH**. ISDOC se nově validuje proti **oficiálnímu XSD 6.0.1** v testu — odhalilo 8 skutečných porušení, takže každý dřívější ISDOC byl odmítnutelný. Fakturační profil sbírá zemi (fallback `CZ`). Zbývá jen reálný import do Pohody (licence) |
| Modul `accounting` — export dokladů do Pohody a ISDOC | **hotovo** | vlna 2.11 | [detail](2026-07-30-accounting-export.md); první nový **premium** modul. Pohoda XML jako dávka za období, ISDOC 6.0.1 jako ZIP (jeden soubor per doklad, typ v názvu — faktura a dobropis mohou nést stejné číslo), plus jednotlivý doklad z detailu. Výběr přes `DocumentLedger` (DUZP, jen daňové doklady), `findTaxDocument` má **povinný typ**. Klíčový nález review: snímek nese ceny **s DPH**, obě cílová pole jsou **bez DPH** — `DocumentLines` převádí přes `TaxRate::net()` celočíselně, jinak by import seděl o ~21 % výš; snímek navíc nenese dopravu, takže se dopočítává řádek „Doprava a poplatky“ (tatáž díra je v zákaznickém PDF — dluh v `Modules/Docs`). Selhává hlasitě: neznámá sazba = 422 se jménem dokladu, kontrolované `ZipArchive` návratové hodnoty, prázdné období = hláška bez souboru, audit až po úspěchu. Reálný import do Pohody a validace ISDOC XSD = pre-deploy |
| Modul `feeds` — XML feedy Heureka a Zboží.cz | **hotovo** | vlna 2.9 | [detail](2026-07-28-feedy.md); nový modul `feeds` (**base** — feed do Heureky je v ČR podmínka prodeje, ne nadstandard), bez `requires`, runtime gate `ShopModules`. `/feed/heureka.xml` a `/feed/zbozi.xml` na doméně nájemce, XML na request + cache 1 h (vzor `SitemapController`); **vypnutý feed i vypnutý modul vracejí 404**, ne prázdné XML (prázdný feed = porovnávač odstraní celý katalog). Varianta = samostatný `SHOPITEM` se sdíleným `ITEMGROUP_ID` a osami v `PARAM`, `ITEM_ID` prefixovaný id produktu; cena vždy z `catalogPrice()` (tedy včetně akce z 2.7), do feedu jen `published()` produkty. `CATEGORYTEXT` z tabulky `feed_category_mappings` (vlastní tabulka, ne sloupce na `categories`), prázdné degraduje na vlastní strom. Blok `DELIVERY` z `ShippingOptions`; bez modulu `shipping` chybí celý. Admin `/admin/m/feeds` (zapnutí, dodací lhůta, mapování) invaliduje cache. Google Merchant, Glami/Favi, import číselníků a per-product opt-out = `docs/future/feedy-dalsi-kroky.md` |
| Nastavení modulů (nájemce) + složení tarifů (superadmin) | **hotovo** | vlna 2.10 | [detail](2026-07-29-nastaveni-modulu.md); `SettingsService::set()` dosud nikdo nevolal — sedm nastavení modulu `docs` šlo měnit jen v kódu. Nově `SettingsSchema`/`SettingsField` nad manifestovým schématem (starý tvar „klíč → string pravidel" se dál parsuje, pole **bez** pravidel shodí `modules:sync`), `setMany()` je all-or-nothing, `all()` slévá defaulty ze schématu. Jedna generická obrazovka `/admin/nastaveni/moduly/{modul}` gatovaná manifestovým `settings_permission` (404 na neběžící modul, 403 bez práva). Nové nastavení: `products.variant_display` (přesun z `tenant_theme` + drop sloupce), `checkout.min_order_total`/`guest_checkout` (server-side v `details()` i `place()`, minimum se měří na zboží po slevě bez dopravy), `orders.number_prefix` (platí od další objednávky). Superadmin `/superadmin/tarify` skládá tarif z modulů; `PlanModuleReconciler` rekonciluje **živě zapnutou sadu** všech tenantů tarifu (kill-switchnutý modul přeskočí, core nikdy nevypne, audit per tenant), odebrání vyžaduje důvod podle spočítaného dopadu. Ruční ověření na demu zbývá; rekonciliace běží synchronně v requestu |
| Modul `packeta` — Zásilkovna (výdejní místa, podání, štítky, tracking) | **hotovo** | vlna 2.5 | [detail](2026-07-27-zasilkovna.md); jádrové kontrakty `Carrier`/`CarrierRegistry`/`PickupPointCatalog`/`ShipmentBook` (vzor `PaymentGatewayRegistry` z 1.4), netenantový katalog `pickup_points` + denní `packeta:sync-points`, server-rendered výběr místa bez JS (widget jen ostrůvek načítaný na kliknutí), `carts.pickup_point_code` + snapshot v objednávce, idempotentní podání přes CAS claim + staleness reclaim, štítky streamované bez ukládání, expediční fronta `/admin/m/packeta/expedice`; `shipping_methods.settings` nově `encrypted:array`. AK §16.5 (další dopravce bez zásahu do checkoutu) **nesplněné**, další dopravci = `docs/future/zasilkovna-dalsi-dopravci.md` |
| Modul `discounts` — slevové kupóny a automatická pravidla | **hotovo** | vlna 2.6 | [detail](2026-07-28-slevovy-engine.md); jádrový kontrakt `DiscountEngine` + guest-safe null bindingy (vzor `PaymentGatewayRegistry`/`CarrierRegistry`), premium modul `discounts` (klíč se odchyluje od spec `coupons`), `DiscountAllocator` rozpustí slevu do řádků košíku i objednávky s haléřovou přesností (capacity-aware), typy procento/pevná částka/doprava zdarma, cíl kategorie/produkty, limity globální i na e-mail, nejvýš jeden kupón + `combinable` automatická pravidla, čerpání v transakci s odpisem skladu a vrácení gated na `returnStock`, objednávka za 0 Kč settluje bez brány, poznámka o slevě na faktuře, admin `/admin/m/discounts`. Final review větve: `release()` je compare-and-swap + `cancel()` bere zámek na objednávce, kupónový endpoint throttluje 10/min, odmítnutý kód se maže na renderu a odeslání odmítne objednávku na **jakýkoli** důvod odmítnutí (nikdy tichá plná cena). Nesený dluh **AK 4 uzavřen vlnou 2.7** (`tax_rate_id` je pro plátce DPH povinné, existující metody dostaly sazbu backfillem). Akční ceny produktu + 30denní nejnižší cena hotové ve vlně 2.7; dárkové poukazy, dávkové kódy a přepočet slevy při editaci = `docs/future/slevy-dalsi-kroky.md` |
| Modul `checkout` — košík, pokladna, odeslání objednávky | **hotovo** | §3.1, §16.3 | [detail](2026-07-21-checkout.md); Blade SSR + progressive enhancement, funguje bez JS; cenová autorita `ProductCatalog`, ne `cart_items.unit_price`; SPAYD QR pro bankovní převod; online platba kartou přes bránu (vlna 1.4) redirectuje na `PaymentGatewayRegistry` |
| Modul `orders` — perzistence, admin, dvojitý stavový automat | **hotovo** | §16.4 | [detail](2026-07-21-checkout.md); idempotentní odeslání, odpis skladu v téže transakci, edice s deltou skladu, ruční založení, storno; historie objednávek v účtu zákazníka hotová |
| Modul `shipping` — způsoby dopravy a platby, matice | **hotovo** | §16.5 | admin-only + storefront options renderuje checkout; kontrakty `ShippingOptions`/`PaymentOptions` s guest-safe null bindingy; účet pro QR šifrovaný (`encrypted:array`), adminovi jen maskovaný; prázdná řada matice = všechny platby povoleny; provider `comgate` s maskovanými credentials přidán vlnou 1.4 |
| Modul `payments` — online brána Comgate, návrat, webhook, expirace | **hotovo** | §16.6 | [detail](2026-07-21-payments.md); registry/driver (víc bran per tenant), verify-before-trust, idempotentní webhook mimo CSRF, `OrderSettlement` kontrakt, expirační job vrací sklad; jen Comgate driver, GoPay/Stripe = design-for |
| Modul `docs` — faktury, PDF, číselná řada, e-mail | **hotovo** | §16.6 | [detail](2026-07-22-docs.md); base modul, `DocumentIssuer`/`DocumentBook` kontrakty, immutable doklad, auto-vystavení přes doménový event (`order.paid`/`order.shipped`, `DB::afterCommit`), dompdf, plátce/neplátce render distinkce |
| Modul `docs` — dobropis, proforma, CSV VAT export, roční číslování | **hotovo** | §16.6 | [detail](2026-07-22-docs-1-6.md); registry + `DocumentWriter`, dobropis (plný storno, gated, bez QR), proforma (nedaňová, QR), export dle DUZP (CSV formula injection ošetřena) |
| Modul `storefront` — layout, homepage, hledání, chybové stránky | **hotovo** | §4.1.1 | [detail](2026-07-20-storefront-katalog.md) |
| Veřejný katalog — kategorie, produkt, řazení a filtr bez JS | **hotovo** | §16.1, §16.2 | bez košíku |
| SEO výstupy — canonical, OG, JSON-LD, sitemap, robots, 301, 410 | **hotovo** | §3.1, §15.3 | |
| Page cache storefrontu (§15.6) | **hotovo** — etapa 1 (aplikační vrstva) | §15.6 | [detail](2026-08-03-page-cache.md); vlna 3.0. Jádrová infrastruktura `app/Core/PageCache/` (ne modul), opt-in middleware `page-cache:{dimenze}` za `StartSession`; klíč `page:{tenant}:{gen-stamp}:{path}:{qs-hash}` s query whitelistem; invalidace generačními čítači ve sloupcích `tenants` (`page_gen_catalog/content/theme`, ne cache store — evikce by čítač vrátila na 1 a obživla staré stránky) přes observery modelů + tři explicitní bumpy tam, kde zápis obchází Eloquent (odpis skladu na hranici skladem/vyprodáno, reorder kategorií, admin flush); CSRF token bezpečně vyměněný za značku (`DynamicTokens`) a vrácený čerstvý při servírování; přihlášený zákazník cache vždy obchází. Sjednocuje sitemap (2.9) a feedy (2.9) pod stejný mechanismus; 404 na neexistující cestě cachuje `RedirectResponder` (výsledek hledání v `redirects`), ne middleware routy. Etapa 2 (statický soubor) a etapa 3 (data/fragment cache) odloženy na nasazení. 96 nových testů (224 assertions) |
| Modul `customers` — registrace, přihlášení, reset hesla, verifikace, účet, admin + GDPR výmaz | **hotovo** | §6.7, §15.1 | čtvrtý guard `customer` nad tenant-scoped tabulkou (unikátní `(tenant_id, email)`); vlastní tenant-scoped tokeny místo Laravelího password brokeru; verifikace e-mailu se nikde nevynucuje (čeká na rozhodnutí etapy `checkout`); historie objednávek v účtu hotová, čte přes `OrderBook::forCustomer`/`findForCustomer` s kontrolou vlastnictví |
| Onboarding — registrace → wizard → e-shop | **hotovo** | §3.1, §6.0 | [detail](2026-07-22-onboarding-billing.md); `TenantProvisioner`, subdoména s live checkem, cross-host signed auto-login, „Moje e-shopy" |
| Trial lifecycle — scheduler | **hotovo** | §9 | `billing:sweep-lifecycle` (`NotTenantAware`), config `trial_days`/`grace_days`, trial→past_due→suspended + e-mail |
| Platformní billing — fakturační ledger nájemci | **hotovo** | §9 | [detail](2026-07-22-onboarding-billing.md); netenantový `platform_invoices`, `PlatformInvoiceWriter` (idempotence per období, VAT split, PDF), stažení superadmin/nájemce |
| Platformní billing — reálné inkaso (Stripe) | **hotovo** | §9 | [detail](2026-07-22-stripe-subscription.md); Stripe Billing (Checkout + Portal), webhook-driven aktivace na `invoice.paid`, idempotentní per období, sweeper skipuje Stripe-managed tenanty, superadmin manuální aktivace retirovaná |
| Platformní billing — roční interval + upgrade/downgrade tarifu | **hotovo** | §9, §13 | [detail](2026-07-23-deferred-billing.md); `plan_prices` (plan×interval), výběr intervalu v checkoutu, změna tarifu přes Billing Portal + `TenantPlanSwitcher` (rekonciliace modulů), idempotence dokladu per Stripe invoice id, `invoice.paid` bere částku/tarif z faktury; Portal konfigurace + 4 Price ids = deploy |
| Fakturační profil nájemce | **hotovo** | §16.6 | jádrová obrazovka `/admin/nastaveni/fakturace`, banner, dodavatel pro docs + odběratel pro platformní fakturu |
| Šablona storefrontu + branding nájemce | **hotovo** | vlna 2.2 | [detail](2026-07-25-storefront-theme.md); jedna čistá šablona, přebarvení všech storefront stránek, `tenant_theme` (logo/favicon/barvy → CSS proměnné, cache-safe), admin „Vzhled" s kontrolou kontrastu; bloková homepage = 2.3, další šablony = future |
| Bloková homepage (page builder) | **hotovo** | vlna 2.3 | [detail](2026-07-26-blokova-homepage.md); 5 typů bloků (`hero`/`product_row`/`category_grid`/`text`/`banner`), `homepage_blocks` v modulu storefront, render Blade SSR bez JS, editor Inertia `admin.storefront.homepage.*`, seed `DefaultHomepage` (provisioner+backfill), URL guard + raster-only + sanitizace, WCAG 2.2 AA audit |
| Vlastní domény nájemců + automatické TLS | **hotovo** | vlna 2.1 | [detail](2026-07-23-custom-domains.md); DNS ověření (TXT+routing) `DomainVerifier`, gating neověřených v resolveru, Caddy ask endpoint `/internal/tls-check` (token+verified-only), cert probe → issued, canonical swap + 301 subdoména→custom, `domains:sweep-pending`, admin obrazovka; infra (Caddyfile, edge DNS, `PLATFORM_TLS_CHECK_TOKEN`) = deploy runbook |
| Tarify / trial / billing | **hotovo** | §3.1 | tabulka `plans` + přiřazení z UI, trial + lifecycle + platformní faktura + reálné inkaso přes Stripe hotové; roční interval a upgrade/downgrade tarifu odloženo |
| Technické dluhy — stale tenant, feed cache, e-mail o stavu, routa stránek | **hotovo** | vlna 3.1 | [detail](2026-08-05-technicke-dluhy.md); `runAs()`/`runWithoutTenant()` přepínají přes `set()` (short-circuit `makeCurrent()` nechával svázanou starou instanci každému volajícímu, který přes ně přepíná — superadmin, oba Stripe handlery, sweeper, `TenantProvisioner`, `AuditLog`); `ShippingMethod` → `Dimension::Catalog`, takže přejmenovaný dopravce už nezůstane ve feedu hodinu; `Tenant::changeStatus()` dispatchne `TenantStatusChanged` přes `DB::afterCommit` a jediný listener mapuje **oba konce** přechodu na zprávu (trial→past_due je expirace, cokoli jiného→past_due je selhaná platba), sweeper své dvě odeslání ztratil; statické stránky přesunuty na `/{page-slug}` přes `Route::fallback()` (ne catch-all, ne blacklist — Laravel ho řadí za všechny routy bez ohledu na pořadí registrace, což je load-bearing, protože `pages` se registruje abecedně před `products` i `storefront`), controller odmítá víc segmentů a padá na `RedirectResponder`, stará cesta 301 bez DB dotazu |
| Cookie lišta + měřicí kódy nájemce | **hotovo** | vlna 3.3 | [detail](2026-08-05-cookie-lista-mereni.md); souhlas se třemi kategoriemi v jádře (`app/Core/Consent/`, lištu má i e-shop, který nic neměří), nový base modul `analytics` (GA4 s Consent Mode v2, Sklik, Meta Pixel, Heureka Ověřeno zákazníky). **Server renderuje konfiguraci, nikdy rozhodnutí** — měřicí id jsou per tenant a smějí do cachovaného HTML, souhlas je per návštěvník a nesmí; test hlídá, že HTML je byte-identické pro nerozhodnutého, souhlasícího i odmítajícího, takže page cache z 3.0 zůstává nedotčená. GA4 se **nenačítá** s „denied", ačkoli to Google doporučuje: požadavek stejně dorazí a nese IP. Tlačítka „Přijmout vše" a „Odmítnout vše" mají shodné třídy (test na markup — nerovnocenná volba znamená neplatný souhlas). `SettingsField` dostal příznak `secret` (šifrování, maskování, keep-on-update) kvůli Heureka klíči. **Chování JS neověřuje automatický test** — čeká na Playwright ve 3.4 |
| Playwright E2E | **hotovo** | vlna 3.4 | [detail](2026-08-06-e2e-playwright.md); 22 testů v `e2e/`, `npm run e2e`, vlastní CI job. Souhlas a měření (7 scénářů — zavírá mezeru z 3.3), nákup bez JS (AK §16.3, poprvé skutečně proklikané), nákup s JS včetně ostrůvku variant a konverze, axe nad 7 stránkami. **Obě premisy vlny neplatily:** omezení certifikátu se týká `curl` přes HTTPS, ne headless prohlížeče na HTTP, a Chromium doménu bez TLD u explicitní `http://` URL s portem zvládne — sada proto jede na `obchod.droidshop` a nepotřebuje žádný nový záznam v `/etc/hosts`. Gate souhlasu byl dočasně porušen, aby se ověřilo, že sada umí zčervenat; nosné jsou scénáře **odmítnutí**, ne „před rozhodnutím" |
| Design handoff | prázdné | `docs/design-droidshop/` | |

## Odchylky od produktové specifikace

Detail a odůvodnění: [`2026-07-19-tenancy-jadro.md`](2026-07-19-tenancy-jadro.md) sekce Odchylky.

Nejdůležitější:

1. `SESSION_DOMAIN` je `null` (host-only cookie) — drží session tenanta na jeho doméně.
2. `past_due` nechává storefront běžet — nechceme trestat zákazníky nájemce za jeho nezaplacenou fakturu.
3. `tenants.plan_id` je nullable — onboarding zakládá tenanta před výběrem tarifu.
4. **Inertia stránky modulů leží v core stromu** (`resources/js/Pages/Modules/<Modul>/`), ne uvnitř modulu — Inertia view finder neumí namapovat krátký název na cestu uvnitř modulu. Detail: [`2026-07-20-katalog-jadro.md`](2026-07-20-katalog-jadro.md).
5. **Řazení kategorií je tlačítky ↑/↓**, ne drag&drop podle §16.2 — tažení nejde ovládat klávesnicí (WCAG 2.1.1).
6. **Vyloučení košíku a pokladny z page cache je explicitní hlavička `Cache-Control: private, no-store`**, ne pravidlo na úrovni routy — page cache jako mechanismus ještě neexistuje. Detail: [`2026-07-21-checkout.md`](2026-07-21-checkout.md).
7. **Stripe webhook je `/superadmin/stripe/webhook`**, ne `/platform/stripe/webhook` z návrhu specu — sedí do existující `/superadmin/*` konvence pro netenantové platformní routy. Paid-through datum předplatného reuse `trial_ends_at` místo vlastního sloupce. Detail: [`2026-07-22-stripe-subscription.md`](2026-07-22-stripe-subscription.md).
8. **Modul slev má klíč `discounts`, ne `coupons`** ze spec §909 — obsluhuje i automatická pravidla bez kódu. Pole pro slevový kód je i v pokladně, ne jen v košíku; obecný `PriceModifier` řetěz ze spec §260 se nestaví. Detail: [`2026-07-28-slevovy-engine.md`](2026-07-28-slevovy-engine.md).

## Známá omezení, na která se narazí dřív než na cokoliv jiného

- **`curl` na subdoménách potřebuje `-k`** — OpenSSL nebere wildcard `*.droidshop` nad jedinou úrovní. Týká se **jen HTTPS**: kontrolní seznam ve `storefront-rendering.md` používá `curl` proti HTTPS. **Playwright to neblokovalo** (vlna 3.4) — E2E sada mluví prostým HTTP proti `artisan serve` a běží na `obchod.droidshop` bez jakéhokoli certifikátu.
- **Platformní joby musí implementovat `NotTenantAware`** — jinak je tenant-aware fronta tiše zahodí.
- **Hledání běží přes `LIKE '%term%'` nad `products.search_text`** — index se nepoužije, a **je to rozhodnutí, ne dluh**: 3,2 ms na base tarifu a 31 ms na stropu premium, kde je 5000 produktů maximum, které tarif dovolí. Přepis na FULLTEXT byl zamítnut dvakrát — 2026-08-05 po měření (přinesl by regresi kvality: hledá celá slova, ne substring) a 2026-09-03 znovu, protože InnoDB neaktualizuje FULLTEXT index do commitu a celá testová sada jede v transakcích. Detail a podmínka, za které se k tomu vrátit: [`docs/decisions/02-katalog-a-produkty.md`](../decisions/02-katalog-a-produkty.md).
- **Page cache podle §15.6 je hotová jen v aplikační vrstvě (vlna 3.0).** Middleware ušetří render a DB dotazy, ale bootování Laravelu (~30–60 ms) zůstává — to sundá až statický soubor servírovaný web serverem (etapa 2), který čeká na VPS a na rozhodnutí o CSRF u staticky servírovaných stránek.
- **Soft-deleted produkty dál počítají do `storage_mb`** — obrázky zůstávají, aby šel produkt obnovit a staré objednávky ho zobrazily.
- **Kill switch přebíjí i core moduly** — vypnutí core modulu vezme e-shopům základní funkčnost. Je to záměr (nouzová brzda), ne chyba.
- ~~**Stav tenanta se mění bez e-mailu nájemci**~~ — uzavřeno vlnou 3.1 (`TenantStatusChanged` + `SendTenantStatusMail`).

## Otevřené chyby

Žádné v `docs/superpowers/errors/`.
